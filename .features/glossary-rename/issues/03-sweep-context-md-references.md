---
status: done
category: enhancement
type: AFK
---

# Sweep remaining live CONTEXT.md references to GLOSSARY.md

> *This was generated by AI during to-issues.*

## Parent

`.features/glossary-rename/PRD.md`

## What to build

Mechanical reference sweep across every remaining live framework surface that still mentions `CONTEXT.md`. After this slice, no non-archived markdown file in the repo refers to `CONTEXT.md` by name — every such reference has been rewritten to `GLOSSARY.md`. Tautological parentheticals like `` `CONTEXT.md` (domain glossary) `` are removed in the same pass (the filename is now the description).

Files in scope:

- `framework/lifecycle.md` — including the ASCII tree on line 29, the prose mentions, the pipeline table rows under "Skill / Reads / Writes", and the project-level artifact list.
- `framework/inbox.md` — the "lives at the project root, parallel to `CONTEXT.md`" line.
- `framework/agents/review-issue.md` — the "reads … `CONTEXT.md`" line.
- `framework/agents/review-feature.md` — the "reads … `CONTEXT.md`" line.
- `README.md` — the introduction paragraph and the `[CONTEXT.md](./CONTEXT.md)` link.
- `CLAUDE.md` — the "Domain vocabulary and architectural decisions live in `CONTEXT.md` …" line.
- `installer/templates/claude-md-snippet.md` — the matching line that ships to new consumers.

`.features/.archived/` is left untouched per the PRD (out of scope; archived snapshots remain a point-in-time record).

## Acceptance criteria

- [ ] Every file listed above references `GLOSSARY.md` (not `CONTEXT.md`).
- [ ] Tautological parentheticals such as `` `CONTEXT.md` (domain glossary) `` are removed in the same edit.
- [ ] From the repo root, `grep -rn "CONTEXT.md" --include='*.md'` returns only matches inside `.features/.archived/`.
- [ ] No occurrences of `CONTEXT-MAP` remain outside `.features/.archived/`.
- [ ] `README.md`'s link to the vocabulary file resolves (`./GLOSSARY.md`).
- [ ] The installer template ships `GLOSSARY.md` to new consumers (`installer/templates/claude-md-snippet.md` references `GLOSSARY.md`).

## Blocked by

- `glossary-rename/01` — the global `grep -rn "CONTEXT.md"` criterion only passes once the dogfood root file has been moved to `GLOSSARY.md`.
- `glossary-rename/02` — the producer-side spec and skill prompt are rewritten by issue 02; the `CONTEXT-MAP` grep criterion only passes once 02's deletions have landed.

## Agent Brief

> *This was generated by AI during triage.*

**Summary:** Rewrite every live `CONTEXT.md` reference in framework documentation, the consumer-facing README/CLAUDE.md, and the installer template to `GLOSSARY.md`; remove tautological `(domain glossary)` parentheticals in the same pass; leave `.features/.archived/` untouched.

**Current behavior:**
Multiple framework surfaces still name `CONTEXT.md` directly: the lifecycle doc's ASCII tree, prose, pipeline table rows under "Skill / Reads / Writes", and project-level artifact list; the inbox doc's "lives at the project root, parallel to `CONTEXT.md`" line; both review agents' "reads … `CONTEXT.md`" line; the README's introduction paragraph and `[CONTEXT.md](./CONTEXT.md)` link; the project `CLAUDE.md`'s "Domain vocabulary and architectural decisions live in `CONTEXT.md` …" line; and the installer template that ships the same line to fresh consumers. Several of these references decorate the filename with a tautological parenthetical like `` `CONTEXT.md` (domain glossary) `` whose explanatory value disappears once the filename itself states what's inside.

**Desired behavior:**
Every live (non-archived) markdown file in the repo references `GLOSSARY.md` and only `GLOSSARY.md` for the vocabulary artifact. `(domain glossary)` parentheticals that follow ` `CONTEXT.md` ` / ` `GLOSSARY.md` ` are removed in the same edit — the filename is now the description, and the parenthetical becomes redundant. The README's link target resolves to `./GLOSSARY.md`. The installer template ships the new name to new consumers without any migration logic. No occurrence of `CONTEXT-MAP` survives outside `.features/.archived/`. `.features/.archived/` is preserved as a point-in-time record and is not touched.

**Key interfaces:**
- **File set** — exactly these live files carry references to update: `framework/lifecycle.md`, `framework/inbox.md`, `framework/agents/review-issue.md`, `framework/agents/review-feature.md`, `README.md`, `CLAUDE.md`, `installer/templates/claude-md-snippet.md`. Sweep `grep -rn "CONTEXT" --include='*.md'` after editing to confirm the only remaining matches live in `.features/.archived/`, `.features/glossary-rename/` (this feature's own PRD and issue files, which describe the rename and necessarily name the old term), and other in-flight feature directories.
- **`docs/formann/` is a symlink farm** — `docs/formann/lifecycle.md`, `docs/formann/inbox.md`, and `docs/formann/domain.md` are symlinks that resolve through `.formann → framework`. Editing `framework/<x>.md` updates the `docs/formann/<x>.md` view automatically; do not edit the symlink targets separately.
- **Tautological parentheticals** — patterns like `` `CONTEXT.md` (domain glossary) ``, `` `CONTEXT.md` (the domain glossary) ``, and any close variant collapse to just `` `GLOSSARY.md` ``. The rewrite of the filename and the removal of the parenthetical happen in the same edit so the file's prose reads naturally after the change.
- **Acceptable lingering references** — `.features/.archived/*` and the in-flight `.features/glossary-rename/*` files (this feature's PRD and issues, which describe the rename itself) are not in scope. The grep criterion is scoped accordingly: matches in those directories are expected and do not fail the criterion.

**Acceptance criteria:**
- [ ] Every file listed above (`framework/lifecycle.md`, `framework/inbox.md`, `framework/agents/review-issue.md`, `framework/agents/review-feature.md`, `README.md`, `CLAUDE.md`, `installer/templates/claude-md-snippet.md`) references `GLOSSARY.md` (not `CONTEXT.md`).
- [ ] Tautological parentheticals such as `` `CONTEXT.md` (domain glossary) `` are removed in the same edit; the filename stands alone.
- [ ] From the repo root, `grep -rn "CONTEXT.md" --include='*.md'` returns only matches inside `.features/.archived/` and `.features/glossary-rename/` (this feature's own PRD/issue files describing the rename).
- [ ] No occurrences of `CONTEXT-MAP` remain outside `.features/.archived/` and `.features/glossary-rename/`.
- [ ] `README.md`'s link to the vocabulary file resolves (`./GLOSSARY.md` exists at the repo root after issue 01).
- [ ] The installer template ships `GLOSSARY.md` to new consumers (`installer/templates/claude-md-snippet.md` references `GLOSSARY.md`).

**Out of scope:**
- Renaming the dogfood root vocabulary file or writing ADR 0005 — that is issue 01.
- Renaming `CONTEXT-FORMAT.md`, editing the `grill-with-docs` skill prompt, or editing `framework/domain.md` — that is issue 02.
- Touching `.features/.archived/` snapshots.
- Adding migration logic, deprecation notices, or compat shims in the installer.
- Adding any tests, lint, or enforcement around the rename.

## Comments

### Implementation — 2026-05-16

> *This was generated by AI during implementation.*

**Evidence**

- [x] **"Every file listed above references `GLOSSARY.md`…":** All seven files updated in commit `9b1e8d9`: `framework/lifecycle.md`, `framework/inbox.md`, `framework/agents/review-issue.md`, `framework/agents/review-feature.md`, `README.md`, `CLAUDE.md`, `installer/templates/claude-md-snippet.md`. Verified with `grep -rn "CONTEXT.md" --include='*.md'` (excluding `.formann/`, `.features/.archived/`, `.features/glossary-rename/`) — no matches in any of the seven target files.
- [x] **"Tautological parentheticals such as `` `CONTEXT.md` (domain glossary) `` are removed…":** `grep -n "domain glossary" framework/lifecycle.md` → no matches. The `CONTEXT.md` (domain glossary) parenthetical on former line 41 of `framework/lifecycle.md` was removed; `GLOSSARY.md` stands alone.
- [x] **"From the repo root, `grep -rn "CONTEXT.md" --include='*.md'`…":** Residual matches are only in: `GLOSSARY.md:11` (the `_Avoid_:` redirect line — intentional, from issue 01), `docs/adr/0005-single-glossary-per-consumer.md` (the ADR recording the rename — intentional, from issue 01), `.formann/` (gitignored local install artifact, not committed — confirmed by `git ls-files .formann/`), `.features/glossary-rename/` (this feature's own files). No live instructional file outside those carries a `CONTEXT.md` reference.
- [x] **"No occurrences of `CONTEXT-MAP` remain outside `.features/.archived/`…":** `grep -rn "CONTEXT-MAP" --include='*.md' | grep -v ".features/"` → no matches (excluding `.formann/` which is gitignored).
- [x] **"`README.md`'s link to the vocabulary file resolves (`./GLOSSARY.md`)…":** `ls /repo/GLOSSARY.md` → file exists. `grep -n "GLOSSARY" README.md` → `[GLOSSARY.md](./GLOSSARY.md)` link on line 9. Link target resolves.
- [x] **"The installer template ships `GLOSSARY.md` to new consumers…":** `grep -n "GLOSSARY" installer/templates/claude-md-snippet.md` → match on line 19: `Domain vocabulary and architectural decisions live in \`GLOSSARY.md\` and \`docs/adr/\`.`

**What changed:**

One commit `9b1e8d9` on `glossary-rename`:
- `framework/lifecycle.md` — ASCII tree entry, two prose paragraphs, pipeline stage descriptions (Grill, PRD, Slice, Triage, Implement), Reads/Writes table (five rows), and document-map entry all updated from `CONTEXT.md` to `GLOSSARY.md`. Tautological `(domain glossary)` parenthetical removed from the project-level artifacts sentence.
- `framework/inbox.md` — "lives at the project root, parallel to `CONTEXT.md`" → `GLOSSARY.md`.
- `framework/agents/review-issue.md` — "reads … `CONTEXT.md`" line → `GLOSSARY.md`.
- `framework/agents/review-feature.md` — "reads … `CONTEXT.md`" line → `GLOSSARY.md`.
- `README.md` — `[CONTEXT.md](./CONTEXT.md)` link → `[GLOSSARY.md](./GLOSSARY.md)`.
- `CLAUDE.md` — "Domain vocabulary and architectural decisions live in `CONTEXT.md`" → `GLOSSARY.md`.
- `installer/templates/claude-md-snippet.md` — matching line → `GLOSSARY.md`.

**Decisions made:**

- `.formann/` files were not edited. They are a gitignored local install artifact (confirmed not committed); in a normal consumer they would be live symlinks resolving through `framework/`, and would automatically reflect the updated content. Editing them separately would be contrary to the brief's explicit note on the symlink farm.
- `GLOSSARY.md:11` and `docs/adr/0005` retain `CONTEXT.md` mentions — both are intentional artifacts from issue 01 (the glossary's own `_Avoid_:` redirect and the ADR recording the rename). Issue 02's reviewer established the same precedent for the ADR; the same reasoning applies here.

### Review (AFK gate) — 2026-05-16

> *This was generated by AI during review.*

## Review: glossary-rename/03 — Sweep remaining live CONTEXT.md references to GLOSSARY.md

**Scope resolved:** Commit `9b1e8d9` on branch `glossary-rename`. Seven files changed: `/repo/CLAUDE.md`, `/repo/README.md`, `/repo/framework/agents/review-feature.md`, `/repo/framework/agents/review-issue.md`, `/repo/framework/inbox.md`, `/repo/framework/lifecycle.md`, `/repo/installer/templates/claude-md-snippet.md`.

---

### Evidence-check findings

**AC3 — the grep criterion is stated with a scope that the implementation slightly exceeds, but the excess is legitimate.**

The original criterion (issue body, line 35) says the grep should return "only matches inside `.features/.archived/`." The agent brief version (line 66) extends this to also allow `.features/glossary-rename/`. The actual result includes two additional locations: `GLOSSARY.md:11` and four lines of `/repo/docs/adr/0005-single-glossary-per-consumer.md` — neither is covered by either criterion statement.

Both of those files were created by issue 01, not issue 03. `GLOSSARY.md:11` is the `_Avoid_:` alias line in the Glossary term entry, which is intentionally the authoritative redirect for the old name — removing `CONTEXT.md` from that line would break the alias contract. The ADR records the rename decision and explicitly names what was renamed. The implementor documents this reasoning in the Evidence block and the Decisions section.

This is a criterion-wording gap from issue 01, not a defect introduced by this commit. The references are semantically correct and their presence is necessary. The Evidence block accurately describes what `grep` returns — the claim is not false, it just correctly identifies that these two files fall outside the criterion's stated scope while explaining why they are intentional. No action needed from this issue.

**All other evidence claims verified as accurate:**

- All seven files are changed in `9b1e8d9` — confirmed by `git show --stat`.
- `grep -rn "CONTEXT.md" --include='*.md'` on the current tree returns zero matches in any of the seven target files.
- No `CONTEXT-MAP` references exist in any tracked file outside `.features/` (the `.formann/` occurrences are in a gitignored, untracked directory confirmed by `git ls-files .formann/` returning zero results).
- `README.md` line 9 contains `[GLOSSARY.md](./GLOSSARY.md)` and the file `/repo/GLOSSARY.md` exists.
- `installer/templates/claude-md-snippet.md` line 19 contains `GLOSSARY.md` — confirmed.
- The tautological `(domain glossary)` parenthetical is gone from `framework/lifecycle.md` line 41 — the `CONTEXT.md` (domain glossary) text now reads simply `GLOSSARY.md` with no parenthetical. Confirmed by direct read and by `grep "domain glossary" framework/lifecycle.md` returning no matches. (Other occurrences of "domain glossary" as a concept in other skill files — triage, implement, to-issues, etc. — are prose descriptions of what to consult, not the parenthetical pattern the criterion targets, and are out of scope for this issue.)

---

### No bugs or intent-check issues found.

The seven-file sweep is complete and correct. `framework/lifecycle.md` has all locations updated: the ASCII tree (line 29), the project-level artifacts sentence (line 41, with tautological parenthetical removed), all five pipeline stage descriptions (lines 54, 57, 60, 63, 66), all five pipeline table rows (lines 78–82), and the document-map entry (line 166).

---

### Verification summary

Verified:
- Commit `9b1e8d9` changes exactly the seven files listed in the issue.
- All seven target files contain zero `CONTEXT.md` or `CONTEXT-MAP` references.
- All tracked non-`.features/` non-`.formann/` markdown files with `CONTEXT.md` are `GLOSSARY.md` and `docs/adr/0005` — both from issue 01, both intentional.
- `.formann/` is fully gitignored (zero tracked files confirmed via `git ls-files`).
- `GLOSSARY.md` exists at the repo root.
- `installer/templates/claude-md-snippet.md` line 19 references `GLOSSARY.md`.
- The `(domain glossary)` tautological parenthetical is absent from all seven target files.
- `CONTEXT-MAP` has zero occurrences in any tracked file outside `.features/`.
- `framework/lifecycle.md` coverage: ASCII tree, prose, all five pipeline stage descriptions, all five table rows, document-map entry — all updated.

**Pending human verification:** None. All criteria in the Evidence block are machine-checkable and were verified against the current tree.
