---
status: done
category: enhancement
type: AFK
---

# Rename dogfood vocabulary file to GLOSSARY.md and record ADR 0005

> *This was generated by AI during to-issues.*

## Parent

`.features/glossary-rename/PRD.md`

## What to build

Stand up the new canonical artifact at the consumer (i.e., Formann itself) root and capture the decision in an ADR. After this slice:

- The file at the repo root is named `GLOSSARY.md`, with `git mv` history so `git log --follow GLOSSARY.md` traces back to the original `CONTEXT.md` commits.
- Inside the file, the inner H2 currently labelled `## Language` is renamed to `## Terms`.
- A `Glossary` term entry is added, defining what a glossary is in Formann and listing `_Avoid_: context, CONTEXT.md, context map, ubiquitous language doc`. The entry sits in whichever existing cluster fits best (the PRD groups it with framework-structure / docs concepts).
- The file continues to demonstrate the format authoritatively — Relationships, Example dialogue, and Flagged ambiguities sections remain.
- A new ADR `docs/adr/0005-single-glossary-per-consumer.md` exists, written in the same shape as `0001`–`0004`, recording both the rename and the deliberate scope reduction (dropping multi-context). The maintainer landing on the ADR finds the answer to "why GLOSSARY?" and "what about bounded contexts?".

This is doc-shaped work; per the PRD no automated tests are added. Verification is by inspection.

## Acceptance criteria

- [ ] `GLOSSARY.md` exists at the repo root; `CONTEXT.md` is gone.
- [ ] `git log --follow GLOSSARY.md` traces back to the original `CONTEXT.md` history (file moved with `git mv`, not delete-plus-add).
- [ ] Inside `GLOSSARY.md`, the H2 currently named `## Language` is renamed to `## Terms`; existing term entries are unchanged in content.
- [ ] `GLOSSARY.md` contains a `Glossary` term entry whose `_Avoid_:` line lists `context`, `CONTEXT.md`, `context map`, and `ubiquitous language doc`.
- [ ] `GLOSSARY.md` still has Relationships, Example dialogue, and Flagged ambiguities sections.
- [ ] `docs/adr/0005-single-glossary-per-consumer.md` exists, follows the shape of the existing ADRs in `docs/adr/`, and records both the rename and the deliberate scope reduction.

## Blocked by

None — can start immediately.

## Agent Brief

> *This was generated by AI during triage.*

**Summary:** `git mv` the dogfood vocabulary file from `CONTEXT.md` to `GLOSSARY.md`, rename its inner `## Language` heading to `## Terms`, add a self-referential `Glossary` term with an `_Avoid_:` redirect, and record the rename + scope reduction as ADR 0005.

**Current behavior:**
The canonical vocabulary file at the Formann repo root is named `CONTEXT.md`. Its inner H2 holding term entries is labelled `## Language`. The file does not define `Glossary` as a term — there is no in-band redirect from the legacy "context" terminology to the new name. No ADR exists for the rename decision.

**Desired behavior:**
The vocabulary file lives at `GLOSSARY.md` at the repo root with git history preserved across the move. Its inner H2 is `## Terms`. A `Glossary` term entry exists with an `_Avoid_:` line listing `context`, `CONTEXT.md`, `context map`, and `ubiquitous language doc`, so an agent or contributor searching for any of those legacy names lands on the canonical definition. The file still demonstrates the full format authoritatively — Relationships, Example dialogue, and Flagged ambiguities sections remain intact. A new ADR `docs/adr/0005-single-glossary-per-consumer.md` records both halves of the decision: the rename, and the deliberate scope reduction (no multi-context).

**Key interfaces:**
- **Vocabulary file shape** — preserve the existing `**Name**:` / body / `_Avoid_:` term entry format. Only the filename, the `## Language` → `## Terms` heading, and the addition of the `Glossary` term entry change. Other content (existing terms, Relationships, Example dialogue, Flagged ambiguities) stays as-is.
- **Glossary term entry** — joins whichever existing cluster groups Formann's framework-structure / docs concepts (e.g. alongside Skill, Agent, Binding). Body is one sentence describing what a glossary is in Formann; `_Avoid_:` line lists the four legacy aliases verbatim.
- **ADR shape** — follow the existing minimal pattern in `docs/adr/0001`–`0004`: H1 declarative title, a 1–3 sentence paragraph stating the decision, then `## Considered options` (the rejected alternatives — at minimum "keep `CONTEXT.md` as the filename" and "keep multi-context as a supported layout"), then `## Consequences`. The ADR records both the rename and the scope reduction in one document.
- **Git move discipline** — use `git mv CONTEXT.md GLOSSARY.md` as a single staged operation; do not delete-and-recreate. Heading rename and `Glossary` term addition happen as edits to the moved file in subsequent commits (or staged together — the only requirement is that the rename itself is recorded as a rename by git).

**Acceptance criteria:**
- [ ] `GLOSSARY.md` exists at the repo root; `CONTEXT.md` is gone.
- [ ] `git log --follow GLOSSARY.md` traces back to the original `CONTEXT.md` history (file moved with `git mv`, not delete-plus-add).
- [ ] Inside `GLOSSARY.md`, the H2 currently named `## Language` is renamed to `## Terms`; existing term entries are unchanged in content.
- [ ] `GLOSSARY.md` contains a `Glossary` term entry whose `_Avoid_:` line lists `context`, `CONTEXT.md`, `context map`, and `ubiquitous language doc`.
- [ ] `GLOSSARY.md` still has Relationships, Example dialogue, and Flagged ambiguities sections.
- [ ] `docs/adr/0005-single-glossary-per-consumer.md` exists, follows the shape of the existing ADRs in `docs/adr/`, and records both the rename and the deliberate scope reduction.

**Out of scope:**
- Sweeping references to `CONTEXT.md` in other framework files (`framework/lifecycle.md`, `framework/inbox.md`, the review agents, `README.md`, `CLAUDE.md`, installer template) — that is issue 03.
- Renaming `CONTEXT-FORMAT.md` or editing producer-side spec / skill prompt / domain-doc contract — that is issue 02.
- Touching `.features/.archived/` snapshots.
- Adding any backwards-compatibility shim, deprecation notice, or migration logic in the installer.
- Adding automated tests for the rename.

## Comments

> *This was generated by AI during implementation.*

**Evidence**

- [x] **"`GLOSSARY.md` exists at the repo root; `CONTEXT.md` is gone":** `ls /repo/GLOSSARY.md` → exists; `ls /repo/CONTEXT.md` → No such file or directory.
- [x] **"`git log --follow GLOSSARY.md` traces back…":** `git log --follow --oneline GLOSSARY.md` → shows commits `106bd0b`, `8a3dd12` (the rename commit), and `e4e4b5e` (prior `CONTEXT.md` history). File moved via `git mv`, not delete-plus-add.
- [x] **"Inside `GLOSSARY.md`, the H2 currently named `## Language` is renamed to `## Terms`…":** `grep -n "## Terms" GLOSSARY.md` → match on line 5; `grep "## Language"` → no match.
- [x] **"`GLOSSARY.md` contains a `Glossary` term entry whose `_Avoid_:` line…":** `grep -n "Glossary\|_Avoid_.*context" GLOSSARY.md` → `**Glossary**:` on line 9, `_Avoid_: context, CONTEXT.md, context map, ubiquitous language doc` on line 11.
- [x] **"`GLOSSARY.md` still has Relationships, Example dialogue, and Flagged ambiguities sections":** `grep -n "## Relationships\|## Example dialogue\|## Flagged ambiguities" GLOSSARY.md` → matches on lines 74, 83, 88.
- [x] **"`docs/adr/0005-single-glossary-per-consumer.md` exists, follows the shape…":** file exists; contains H1 declarative title, 1-paragraph decision statement, `## Considered options` (two rejected alternatives: keep `CONTEXT.md`, keep multi-context), and `## Consequences`. Shape matches `0001`–`0004`.

**What changed:**

Two commits on `glossary-rename`:
1. `8a3dd12` — `git mv CONTEXT.md GLOSSARY.md` (rename only, so git records a rename at 100% similarity).
2. `106bd0b` — rename `## Language` → `## Terms`, insert `Glossary` term entry at top of the Framework structure cluster, create `docs/adr/0005-single-glossary-per-consumer.md`.

**Decisions made:**

- Placed the `Glossary` term entry at the top of the `### Framework structure` cluster (before `Formann`). It is a structural artifact living at the consumer root, which makes it a natural peer of `Formann`, `Consumer`, and `.formann` in that cluster. No other cluster fits as naturally.
- ADR body leads with the rename and simultaneously records the multi-context retirement, since they are two parts of one decision (per the PRD's framing).

### Review (AFK gate) — 2026-05-16

> *This was generated by AI during runner-side review.*

All six criteria are now verified. Here are the findings.

---

## Verification summary

**Scope resolved:** commits `8a3dd12` and `106bd0b` on branch `glossary-rename`. Files reviewed: `/repo/GLOSSARY.md`, `/repo/docs/adr/0005-single-glossary-per-consumer.md`, existing ADRs `0001` and `0004` (shape reference), issue file `/repo/.features/glossary-rename/issues/01-rename-dogfood-and-record-adr.md`.

### Criterion 1 — `GLOSSARY.md` exists at repo root; `CONTEXT.md` is gone

Clean. `GLOSSARY.md` is present. `ls CONTEXT.md` exits 2 (no such file). Confirmed.

### Criterion 2 — `git log --follow GLOSSARY.md` traces back to original `CONTEXT.md` history

Clean. `git log --follow --oneline GLOSSARY.md` returns three entries: `106bd0b`, `8a3dd12` (the rename commit), `e4e4b5e` (the commit that originally created `CONTEXT.md`). The diff for `8a3dd12` shows `rename from CONTEXT.md` / `rename to GLOSSARY.md` at 100% similarity — git recorded a rename, not a delete-and-add.

### Criterion 3 — `## Language` renamed to `## Terms`; existing term entries unchanged

Clean. The diff for `106bd0b` shows exactly `-## Language` / `+## Terms`. No other existing term entries were modified. The rename was a single-line substitution.

### Criterion 4 — `Glossary` term entry with correct `_Avoid_:` line

Clean. `GLOSSARY.md` lines 9–11 contain:

```
**Glossary**:
The single vocabulary file at the consumer's root (`GLOSSARY.md`) that defines the project's canonical terms and redirects legacy aliases to their replacements.
_Avoid_: context, CONTEXT.md, context map, ubiquitous language doc
```

All four required aliases are present verbatim: `context`, `CONTEXT.md`, `context map`, `ubiquitous language doc`.

### Criterion 5 — Relationships, Example dialogue, and Flagged ambiguities sections remain

Clean. All three sections are present at lines 74, 83, and 88 respectively. Content is intact and unchanged.

### Criterion 6 — `docs/adr/0005-single-glossary-per-consumer.md` exists, follows ADR shape, records both rename and scope reduction

Clean. The file exists. Structure matches `0001`–`0004`: H1 declarative title, a single opening decision paragraph, `## Considered options`, `## Consequences`. Both halves of the decision are recorded: the `CONTEXT.md` → `GLOSSARY.md` rename and the retirement of the multi-context layout. The two rejected options are explicitly named and their rejections are reasoned.

---

### Bug-hunt, intent-check, and codebase-fit findings

No issues found.

- The Glossary term body accurately describes what a Glossary is in Formann at the time of this commit (single file at consumer root, defines canonical terms, redirects aliases). No stale or history-narrating language.
- The ADR body does not use any "previously" / "now" / "improved" phrasing that would become misleading over time.
- The `Glossary` term is placed at the top of the `### Framework structure` cluster, before `Formann`. This placement is coherent — the glossary is a structural artifact of the consumer root, making it a natural peer of `Consumer`, `.formann`, etc. in that cluster.
- No circular dependencies, resource leaks, or other code-path concerns apply (doc-only change).

### Evidence-check

All six `[x]` claims in the Evidence block were independently verified and found accurate. The claimed line numbers (5, 9, 11, 74, 83, 88) match the current file. The claimed commit SHAs (`8a3dd12`, `106bd0b`, `e4e4b5e`) are real and their roles are as described.

### Pending human verification

The issue marks all criteria as `[x]` (no `[ ]` / `[human]` items remain). The issue itself notes "Verification is by inspection" — no automated tests were required and none are absent.

**Overall:** the implementation satisfies all six acceptance criteria with no findings.
