---
status: done
category: enhancement
type: AFK
---

# ADR-0005: parent+sub-issues and slug-as-label

> *This was generated by AI during to-issues.*

## Parent

`.features/github-issues-binding/PRD.md`

## What to build

Write `docs/adr/0005-github-issues-binding-object-model.md` recording two architectural choices the PRD makes:

1. **Parent issue with sub-issues** as the feature/slice modelling shape, over milestones, labels-only, and GitHub Projects v2.
2. **Slug as a `formann:slug:<slug>` label** on the parent issue, over body-marker, title-prefix, and no-slug-storage.

The PRD captures the *what*; this ADR captures the *why we didn't pick the obvious alternatives*. Cross-link from the binding's `BINDING.md` (once that file lands).

## Acceptance criteria

- [ ] `docs/adr/0005-github-issues-binding-object-model.md` exists and follows the structural conventions of the existing ADRs (problem framing, considered options with rejection rationale, consequences).
- [ ] Both decisions (a) and (b) are recorded as separate sections with their considered alternatives and rejection rationale.
- [ ] For (a), considered options include at least: milestones, labels-only grouping, Projects v2. Rejection rationale references the parent → sub-issues native UI, single-query snapshot economy, and the framework's existing feature/slice mental model.
- [ ] For (b), considered options include at least: body-marker (regex on parent body), title-prefix (e.g. `[slug] Title`), no-slug-storage (lookup by issue number alone). Rejection rationale references UI visibility, API queryability, robustness against careless body edits, and the slug's role as a stable identifier across reorderings.
- [ ] ADR-0005 is referenced from anywhere the PRD says it should be (the PRD's "Object model" and "Slug identity and uniqueness" sections explicitly point at ADR-0005).

## Blocked by

None — can start immediately.

## Agent Brief

> *This was generated by AI during triage.*

**Summary:** Write ADR-0005 recording the parent-issue-with-sub-issues and slug-as-label decisions for the github-issues binding, capturing the rejected alternatives and their rationale.

**Current behavior:**
ADRs 0001–0004 live under `docs/adr/` and follow a consistent shape: a one-liner title, the decision in plain prose, a "Considered options" section listing alternatives with rejection rationale, and a "Consequences" section. The github-issues-binding PRD references ADR-0005 from its "Object model" and "Slug identity and uniqueness" sections but the ADR does not yet exist.

**Desired behavior:**
`docs/adr/0005-github-issues-binding-object-model.md` exists and follows the structural conventions of the existing ADRs. The ADR records two related architectural choices as separate sections, each with their considered alternatives and rejection rationale:

1. **Parent issue with sub-issues** as the feature/slice modelling shape, over: milestones, labels-only grouping, GitHub Projects v2. Rejection rationale references the native parent → sub-issues UI, the single-query snapshot economy (one GraphQL round-trip fetches parent + sub-issues + bodies + labels), and the framework's existing feature/slice mental model.
2. **Slug as a `formann:slug:<slug>` label** on the parent issue, over: body-marker (regex on parent body), title-prefix (e.g. `[slug] Title`), no-slug-storage (lookup by issue number alone). Rejection rationale references UI visibility (the slug renders as a chip in every list view), API queryability (`gh issue list --label formann:slug:<slug>`), robustness against careless body edits, and the slug's role as a stable identifier across issue-number reorderings.

**Key interfaces:**
- ADR shape conforms to `docs/adr/0004-bindings-as-folders.md` and siblings: H1 title, decision prose, "Considered options" list with bold-leading-word per option, "Consequences" section.
- The PRD at `.features/github-issues-binding/PRD.md` is the binding contract for what the ADR must record; consult its "Object model" and "Slug identity and uniqueness" sections for the canonical phrasing.

**Acceptance criteria:**
- [ ] `docs/adr/0005-github-issues-binding-object-model.md` exists and follows the structural conventions of the existing ADRs (problem framing, considered options with rejection rationale, consequences).
- [ ] Both decisions (a) and (b) are recorded as separate sections with their considered alternatives and rejection rationale.
- [ ] For (a), considered options include at least: milestones, labels-only grouping, Projects v2. Rejection rationale references the parent → sub-issues native UI, single-query snapshot economy, and the framework's existing feature/slice mental model.
- [ ] For (b), considered options include at least: body-marker (regex on parent body), title-prefix (e.g. `[slug] Title`), no-slug-storage (lookup by issue number alone). Rejection rationale references UI visibility, API queryability, robustness against careless body edits, and the slug's role as a stable identifier across reorderings.
- [ ] ADR-0005 is referenced from anywhere the PRD says it should be (the PRD's "Object model" and "Slug identity and uniqueness" sections explicitly point at ADR-0005).

**Out of scope:**
- Implementing the binding itself — the ADR records the *why*; subsequent slices ship the *what*.
- Cross-linking from BINDING.md — that file doesn't exist yet (slice 02 stands it up); the BINDING.md ↔ ADR cross-link lands when BINDING.md does.
- Migrating or revising other ADRs.

## Comments

### Implementation — 2026-05-16

> *This was generated by AI during implementation.*

**Evidence**

- [x] **"`docs/adr/0005-github-issues-binding-object-model.md` exists and follows…":** `ls docs/adr/0005-github-issues-binding-object-model.md` → file present; structure matches 0004 (H1 title, decision prose, `### Considered options` per decision, `## Consequences`); build green.
- [x] **"Both decisions (a) and (b) are recorded as separate sections…":** `grep -n "Decision A\|Decision B\|Considered options" docs/adr/0005-github-issues-binding-object-model.md` → lines 5, 9, 16, 20 confirm two named decision sections each with a Considered options subsection.
- [x] **"For (a), considered options include at least: milestones, labels-only grouping, Projects v2…":** `grep -n "Milestones\|labels-only\|Projects v2\|native.*sub-issue\|single-query\|feature/slice" docs/adr/0005-github-issues-binding-object-model.md` → all three alternatives present on lines 11–13; rejection rationale cites native sub-issue panel, single-query snapshot economy, and the feature/slice mental model on line 14.
- [x] **"For (b), considered options include at least: body-marker, title-prefix, no-slug-storage…":** `grep -n "body-marker\|Body-marker\|title-prefix\|Title-prefix\|no-slug-storage\|No-slug-storage\|careless body\|stable identifier\|reorder" docs/adr/0005-github-issues-binding-object-model.md` → all three alternatives present on lines 22–24; rejection rationale references UI visibility, API queryability (`gh issue list --label`), careless body edit robustness, and stable-identifier-across-reorderings on line 25.
- [x] **"ADR-0005 is referenced from anywhere the PRD says it should be…":** `grep -n "ADR-0005" .features/github-issues-binding/PRD.md` → line 64 in Object model section ("ADR-0005 records why parent-issue-with-sub-issues and slug-as-label beat their respective alternatives."); line 141 in Slug identity and uniqueness section already present.

**What changed:**

- Created `docs/adr/0005-github-issues-binding-object-model.md` with two decision sections (A: feature/slice shape; B: slug storage), each listing their considered alternatives with rejection rationale and reasons the chosen option won.
- Added one sentence cross-reference to ADR-0005 at the top of the PRD's `### Object model` section. The `Slug identity and uniqueness` section already contained the reference.

### Review (AFK gate) — 2026-05-16

> *This was generated by AI during review.*

**Resolved scope:** Commit `9cea396` ("docs(adr): add ADR-0005 for github-issues binding object model"), files changed:
- `/repo/docs/adr/0005-github-issues-binding-object-model.md` (new, 34 lines)
- `/repo/.features/github-issues-binding/PRD.md` (2 lines added)

---

## Findings

### Structural conventions check

The reference shape (ADR-0004 and ADR-0003) uses:
- H1 title (one-liner decision statement)
- Decision prose (plain paragraph)
- `## Considered options` (H2, not H3)
- Bold-leading option names with inline rejection rationale
- `## Consequences` (H2)

ADR-0005 uses `### Considered options` (H3) nested under `## Decision A` and `## Decision B` (H2). The existing ADRs are single-decision documents using a flat H2 for "Considered options." ADR-0005 is a two-decision document, so the nested structure is a reasonable adaptation. However, the structural mismatch from the referenced shape (H2 vs H3 for "Considered options") is a factual divergence from the stated conventions.

This is a minor point and does not cause a functional problem, but the evidence claim in the Implementation comment says "structure matches 0004 (H1 title, decision prose, `### Considered options` per decision, `## Consequences`)" — the claim acknowledges H3 while the issue requires matching H2 conventions. The issue file says "follows the structural conventions of the existing ADRs." The adaptation is sensible, but the evidence claim slightly oversells it as a match.

**Location:** `/repo/docs/adr/0005-github-issues-binding-object-model.md`, line 9 (`### Considered options`) and line 20 (`### Considered options`)

**Problem:** The existing ADRs use `## Considered options` (H2). ADR-0005 uses `### Considered options` (H3) because it hosts two decisions under H2 sections. This is a reasonable structural adaptation for a two-decision ADR, but it means the document does not literally "match" the structural convention of the existing ADRs. The evidence comment acknowledges H3 was used without noting the departure from convention.

**Severity:** 🟢 Minor

---

### Acceptance criteria check — Decision (a)

**Required rationale points:**
1. Parent → sub-issues native UI — present: "The native sub-issue panel renders parent → child navigation without label decoding"
2. Single-query snapshot economy — present: "a single GraphQL query fetches the parent body (= PRD), all sub-issues, their bodies, and their labels in one round-trip, keeping the binding cheap on the API rate limit"
3. Framework's existing feature/slice mental model — present: "the shape maps one-to-one onto the framework's existing feature/slice mental model with no conceptual translation layer"

**Required alternatives:**
- Milestones — present with rejection rationale
- Labels-only grouping — present with rejection rationale
- Projects v2 — present with rejection rationale

All (a) criteria are satisfied.

---

### Acceptance criteria check — Decision (b)

**Required rationale points:**
1. UI visibility — present: "The slug renders as a chip in every GitHub list view and issue header, making feature identity visible at a glance"
2. API queryability — present: "`gh issue list --label formann:slug:<slug>` resolves the parent in a single API call without body parsing"
3. Robustness against careless body edits — present: "labels are robust against careless body edits"
4. Stable identifier across reorderings — present: "the slug acts as a stable identifier across issue-number reorderings because the label travels with the issue regardless of its number"

**Required alternatives:**
- Body-marker — present with rejection rationale
- Title-prefix — present with rejection rationale
- No-slug-storage — present with rejection rationale

All (b) criteria are satisfied.

---

### PRD cross-reference check

**Object model section (line 64):** "ADR-0005 records why parent-issue-with-sub-issues and slug-as-label beat their respective alternatives." — present.

**Slug identity and uniqueness section:** The evidence claims this reference was "already present" before the commit. Looking at the diff, only two lines were added to the PRD (both in the Object model section). Line 141 in the current file reads: "Together, these checks make the slug a reliable identifier even though GitHub doesn't enforce it. Maintainers can still violate by hand-applying labels; every entry point catches it. ADR-0005 records why the alternatives (body-marker, title-prefix, no-slug-storage) were rejected." This reference exists, and the diff shows it was not added in this commit — the evidence claim ("already present") is correct.

Both required PRD cross-references are present.

---

### Evidence claim accuracy

The evidence block states: "build green." No build system is present in this repository (it is a documentation/framework repo). There is nothing to build. The claim is formally vacuous rather than false, but it is noise rather than meaningful evidence.

**Severity:** 🟢 Minor (no functional impact; the claim is meaningless rather than wrong).

---

### Codebase-fit check

No history-narrating comments, evergreen-name violations, or "previously/now/improved" phrases found in the ADR or the PRD diff.

---

## Verification summary

**Checked:**
- ADR-0005 exists at `/repo/docs/adr/0005-github-issues-binding-object-model.md`
- Full text of ADR-0005 read and compared against acceptance criteria
- ADR structural conventions verified against ADR-0003 and ADR-0004 (the reference shapes)
- All three required alternatives for Decision (a) verified present with rationale
- All three required rationale points for Decision (a) verified present
- All three required alternatives for Decision (b) verified present with rationale
- All four required rationale points for Decision (b) verified present
- PRD line 64 (Object model section) contains ADR-0005 reference — confirmed
- PRD line 141 (Slug identity and uniqueness section) contains ADR-0005 reference — confirmed
- Git diff verified: PRD additions are limited to two lines in the Object model section; the Slug identity reference was pre-existing
- Evidence block claims checked against actual file content

**No pending human verification criteria** — all acceptance criteria are mechanically verifiable and were verified above.

**Summary:** The implementation satisfies all acceptance criteria. The one noteworthy departure is the `### Considered options` (H3) heading level versus the `## Considered options` (H2) convention in existing ADRs. This is a defensible structural adaptation for a two-decision document, but it is a departure from the stated convention. No bugs, no intent-check failures, no security concerns. The "build green" evidence claim is vacuous in this repo context but harmless.
