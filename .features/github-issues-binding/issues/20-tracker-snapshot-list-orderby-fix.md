---
status: done
category: bug
type: AFK
---

# Fix `tracker-snapshot --list` GraphQL `orderBy` — invalid `NUMBER` enum value bricks the runner

> *This was generated by AI during to-issues.*

## Parent

`.features/github-issues-binding/PRD.md`

## What to build

The `--list` GraphQL query in `framework/bindings/issue-tracker/github-issues/tracker-snapshot:103-114` orders results by `orderBy: {field: NUMBER, direction: ASC}`. `NUMBER` is not a valid `IssueOrderField` enum value — the only valid values are `CREATED_AT`, `UPDATED_AT`, and `COMMENTS` (verified via GitHub GraphQL introspection on the live API). Live execution rejects the query with `argumentLiteralsIncompatible`; the script exits with `_EXIT_TRANSIENT` after 3 retries. This bricks the AFK runner's feature discovery the moment the binding ships, because `tracker-snapshot --list` is the runner's first call against live GitHub.

The unit tests don't catch this: the `gh` shim at `tests/tracker-snapshot.bats:21-50` serves canned JSON regardless of query content — it never validates GraphQL syntax or enum values. `BINDING.md:101` documents the same broken query, so the contract itself is wrong. The test fixture at `tests/fixtures/list-archived-excluded/recorded-responses/graphql-response.json` happens to return issues already in `#N` order, so the test at `tests/tracker-snapshot.bats:154-163` cannot distinguish an API-side sort from an in-script sort — it's the structural reason the bug slipped past CI.

PRD US-15 (line 38) is the authoritative contract: `tracker-snapshot --list` returns features ordered by issue number ascending. The fix is to drop `orderBy` from the query and sort in jq via `sort_by(.number)` before extracting pairs. The pipeline at `tracker-snapshot:122-133` already projects `.number`, so the sort is trivially cheap (100-element max). `CREATED_AT` is not a safe substitute: transferred issues preserve original `createdAt` while being renumbered, so `CREATED_AT` and `#N` can diverge — only an explicit `.number` sort honors the PRD contract under all conditions.

## Acceptance criteria

- [ ] `framework/bindings/issue-tracker/github-issues/tracker-snapshot`: `orderBy: {field: NUMBER, direction: ASC},` is removed from the `_list_query` block; the jq pipeline that produces `_pairs` sorts the nodes by `.number` before iterating.
- [ ] `framework/bindings/issue-tracker/github-issues/tracker-snapshot:176-177`: the comment claiming order comes from `NUMBER ASC` is updated to describe the in-script sort.
- [ ] `framework/bindings/issue-tracker/github-issues/BINDING.md:98-106`: the documented `--list` query mirrors the script — no `orderBy` clause, with prose noting the in-script `#N` sort.
- [ ] `framework/bindings/issue-tracker/github-issues/tests/fixtures/list-archived-excluded/recorded-responses/graphql-response.json`: the response presents feature issues out of `#N` order (e.g., `#7` before `#3`). `expected.json` stays in `#N` ASC order — proving the in-script sort delivers the contract regardless of input order.
- [ ] `framework/bindings/issue-tracker/github-issues/tests/tracker-snapshot.bats:154-163`: the test name and inline comment no longer claim "query uses `orderBy NUMBER ASC`"; they describe the in-script sort. The test continues to assert the output is `["alpha-feature", "beta-feature"]` (or whatever the slugs are in encounter order after sort).
- [ ] The full bats suite for this binding (`tracker-snapshot.bats`, `body-edit.bats`, `bootstrap-labels.bats`, `sandbox-env.bats`) is green.
- [ ] No other behavior changes: sub-issue ordering (`<slug>` mode) is untouched.

## Blocked by

None — can start immediately.

## Agent Brief

> *This was generated by AI during to-issues.*

**Summary:** Replace a non-existent GraphQL enum with an in-script jq sort. Same observable behavior on test fixtures; correct behavior on real GitHub. Fix the documentation and strengthen the test fixture so this class of bug cannot slip past CI again.

**Current behavior:**

- `tracker-snapshot --list` builds a GraphQL query at `tracker-snapshot:103-114` with `orderBy: {field: NUMBER, direction: ASC}`.
- Live GitHub API rejects the query: `Argument 'field' on InputObject 'IssueOrder' has an invalid value (NUMBER). Expected type 'IssueOrderField!'.`
- The retry loop in `_graphql` (lines 69-86) re-attempts 3 times, then the script exits with `_EXIT_TRANSIENT` (3) and writes `tracker-snapshot: API request failed after 3 attempts` to stderr.
- The `gh` shim used in tests (`tracker-snapshot.bats:21-50`) ignores the query string and serves canned JSON, so all bats tests pass despite the broken query.
- The fixture at `tests/fixtures/list-archived-excluded/recorded-responses/graphql-response.json` presents features in `#N` ASC order already, so the test at `tests/tracker-snapshot.bats:154-163` cannot detect whether sorting happens or not.
- `BINDING.md:101` documents the broken query verbatim, so the binding contract is also wrong.

**Desired behavior:**

- The `--list` query has no `orderBy` clause. (The default ordering GitHub returns is unspecified; the script sorts in-process.)
- After the `gh api graphql` call returns, the jq pipeline sorts the `issues.nodes` array by `.number` ascending before extracting `(number, slug)` pairs.
- `tracker-snapshot --list` runs cleanly against real GitHub and emits a JSON array of slugs in `#N` ASC order.
- The fixture presents nodes out of `#N` order, and the test still passes — proving the in-script sort works.
- `BINDING.md` documents the in-script sort, not the non-existent enum.

**Key interfaces:**

- `framework/bindings/issue-tracker/github-issues/tracker-snapshot` — the `_list_query` heredoc (lines 103-114) and the `_pairs` jq pipeline (lines 122-133). Move sorting from the GraphQL layer into the jq layer with `sort_by(.number)` applied to `.data.repository.issues.nodes` before the `.[]` flatten.
- `framework/bindings/issue-tracker/github-issues/BINDING.md` — the `--list` query example at lines 94-106 and the ordering rules section at lines 540-541.
- `framework/bindings/issue-tracker/github-issues/tests/fixtures/list-archived-excluded/recorded-responses/graphql-response.json` — swap the order of the two feature nodes so the higher `#N` appears first. `expected.json` (slugs in `#N` ASC order) stays as-is.
- `framework/bindings/issue-tracker/github-issues/tests/tracker-snapshot.bats:154-163` — update test name and comment; the assertion stays.

**Why `sort_by(.number)` and not `CREATED_AT`:**

PRD US-15 promises `#N` ASC. The valid `IssueOrderField` values are `CREATED_AT`, `UPDATED_AT`, `COMMENTS`. `CREATED_AT` correlates with `#N` on a fresh repo, but **diverges** after `gh issue transfer` (createdAt preserved, `#N` reassigned) or after a tracker import. The contract is `#N` ASC, so the implementation must sort on `.number`. The 100-element-max payload makes the in-script sort effectively free.

**Acceptance criteria:**

- [ ] `tracker-snapshot --list` succeeds against a real GitHub repo (out-of-scope to automate here, but the implementation must be correct against the live schema).
- [ ] `BINDING.md` no longer mentions `field: NUMBER` or any non-existent enum value.
- [ ] The `list-archived-excluded` fixture presents feature issues out of `#N` order; the bats test for that fixture still asserts the expected `#N` ASC output.
- [ ] All four bats suites for this binding are green.
- [ ] No regression in sub-issue ordering (`<slug>` mode) — the `subIssues` connection's UI-priority order remains untouched.

**Out of scope:**

- The smoke walk artifact at `.runner-state/smoke-runs/2026-05-17-github-issues-binding.md` is still an unfilled skeleton. That's a separate process gap; `github-issues-binding/13` stays `done` per maintainer decision.
- Any change to sub-issue ordering or the `<slug>` query.
- Adding live-API smoke tests to CI (worth doing, but a separate change).

## Comments

### Implementation — 2026-05-18

> *This was generated by AI during implementation.*

**Evidence**

- [x] **"`orderBy: {field: NUMBER, direction: ASC},` is removed…":** `grep -n 'orderBy' framework/bindings/issue-tracker/github-issues/tracker-snapshot` → no matches; the `sort_by(.number)` jq transform is now on the `.data.repository.issues.nodes` array before iterating. Build green (all 41 bats tests pass).
- [x] **"`tracker-snapshot:176-177`: the comment claiming order comes from `NUMBER ASC`…":** comment now reads "because the jq pipeline sorts nodes by `.number` before iterating". `grep -n 'NUMBER ASC' framework/bindings/issue-tracker/github-issues/tracker-snapshot` → no matches.
- [x] **"`BINDING.md:98-106`: the documented `--list` query mirrors the script…":** `grep -n 'orderBy' framework/bindings/issue-tracker/github-issues/BINDING.md` → no matches in the query block; prose note explains in-script `sort_by(.number)` and why `CREATED_AT` is not a safe substitute.
- [x] **"the `list-archived-excluded` fixture presents feature issues out of `#N` order…":** fixture now has `#7` first, `#5` (CLOSED) second, `#3` third. `expected.json` unchanged (`["alpha-feature","beta-feature"]`). Test 10 (`--list: ordered by #N ascending (in-script sort_by(.number))`) passes — proving the sort delivers the contract regardless of API return order.
- [x] **"`tracker-snapshot.bats:154-163`: the test name and inline comment no longer claim…":** test renamed to `"--list: ordered by #N ascending (in-script sort_by(.number))"` with updated inline comment describing out-of-order fixture. Assertion unchanged.
- [x] **"The full bats suite … is green.":** `bats tracker-snapshot.bats body-edit.bats bootstrap-labels.bats sandbox-env.bats` → 41/41 ok.
- [x] **"No other behavior changes: sub-issue ordering (`<slug>` mode) is untouched.":** `grep -n 'subIssues' framework/bindings/issue-tracker/github-issues/tracker-snapshot` → the sub-issue path is unmodified; test 3 (`multi-issue priority ordering — subIssues API order preserved unchanged`) passes.

**What changed:**

- `tracker-snapshot` (`_list_query` heredoc): removed `orderBy: {field: NUMBER, direction: ASC},`.
- `tracker-snapshot` (`_pairs` jq pipeline): changed `.data.repository.issues.nodes[]` to `(.data.repository.issues.nodes | sort_by(.number))[]`.
- `tracker-snapshot` (comment at line 176): updated to describe the in-script sort.
- `BINDING.md` (lines 94-106): removed `orderBy` from the documented query; added prose explaining the in-script sort and why `CREATED_AT` diverges after `gh issue transfer`.
- `BINDING.md` (ordering rules section): updated to state the GraphQL query carries no `orderBy` and the script sorts via `jq sort_by(.number)`.
- `tests/fixtures/list-archived-excluded/recorded-responses/graphql-response.json`: swapped node order so `#7` precedes `#3` (out-of-`#N`-order input).
- `tests/tracker-snapshot.bats:154-163`: renamed test and updated comment; assertion unchanged.

**Decisions made:**

- Used `(.data.repository.issues.nodes | sort_by(.number))[]` rather than inserting a separate `| sort_by(.number)` after the flatten — keeping the sort at the array level is cleaner and avoids re-checking the `state` filter before sorting.

### Review (AFK gate) — 2026-05-18

> *This was generated by AI during runner-side review.*

## Resolved scope

Commits reviewed:
- `30e2aa4` — "github-issues binding: fix --list GraphQL orderBy, sort in jq instead" (the implementation)
- `07dbfb7` — "issue 20: set in-review, add implementation comment" (state transition only)

Files changed by the implementation commit:
- `/repo/framework/bindings/issue-tracker/github-issues/tracker-snapshot`
- `/repo/framework/bindings/issue-tracker/github-issues/BINDING.md`
- `/repo/framework/bindings/issue-tracker/github-issues/tests/fixtures/list-archived-excluded/recorded-responses/graphql-response.json`
- `/repo/framework/bindings/issue-tracker/github-issues/tests/tracker-snapshot.bats`

---

## Verification summary

**Check 1 — `orderBy` removed from `_list_query`.**
Confirmed. The heredoc at line 105 of `/repo/framework/bindings/issue-tracker/github-issues/tracker-snapshot` reads `issues(labels: ["formann:feature"], states: [OPEN], first: 100)` with no `orderBy` argument. `grep` confirms zero matches for `orderBy` in the script.

**Check 2 — `sort_by(.number)` in `_pairs` pipeline.**
Confirmed. Line 122: `(.data.repository.issues.nodes | sort_by(.number))[] |`. The sort is applied at the array level before the `select` filter, which is the correct placement — it sorts before iterating, not after.

**Check 3 — Updated comment at line 175–176.**
Confirmed. Lines 175–176 read:
```
# Emit JSON array of slugs (in encounter order, which matches #N ascending
# because the jq pipeline sorts nodes by .number before iterating).
```
No reference to `NUMBER ASC` remains.

**Check 4 — BINDING.md query block has no `orderBy`; prose explains in-script sort.**
Confirmed. The query block at lines 94–105 has no `orderBy` clause. Line 107 adds a complete prose explanation: the `sort_by(.number)` approach, why `NUMBER` is invalid, and why `CREATED_AT` is not a safe substitute (transferred issues). The ordering rules section at line 565–566 is also updated to match.

**Check 5 — Fixture presents nodes out of `#N` order.**
Confirmed. The fixture at `/repo/framework/bindings/issue-tracker/github-issues/tests/fixtures/list-archived-excluded/recorded-responses/graphql-response.json` now has `#7` (beta-feature, OPEN) first, `#5` (gamma-feature, CLOSED) second, `#3` (alpha-feature, OPEN) third.

**Check 6 — `expected.json` still has slugs in `#N` ASC order.**
Confirmed. `/repo/framework/bindings/issue-tracker/github-issues/tests/fixtures/list-archived-excluded/expected.json` contains `["alpha-feature","beta-feature"]` — i.e., `#3`'s slug before `#7`'s slug, which is `#N` ascending.

**Check 7 — Bats test accurately describes in-script sort.**
Confirmed. Test at line 154 is named `"--list: ordered by #N ascending (in-script sort_by(.number))"`. The inline comment at lines 155–156 accurately describes the fixture as out-of-order (`#7, #5-closed, #3`) and the assertion as proving the in-script sort. The assertions at lines 162–163 explicitly check `.[0] == "alpha-feature"` and `.[1] == "beta-feature"` — positional checks that would fail if the sort were absent.

**Check 8 — Sub-issue path untouched.**
Confirmed. The `subIssues` connection path (lines 200, 238, 241) is identical to its pre-fix form. No `sort_by` is applied there. The comment at line 238–239 explicitly notes the API-returned order is already deterministic and no sort is applied.

**Check 9 — Test 9 (archived-excluded) still passes.**
The test at lines 145–152 uses the same fixture and does a `diff` against `expected.json` via `jq -S` (which normalizes key order but not array order). Since the expected array is `["alpha-feature","beta-feature"]` and the sort delivers that order, the diff passes. The `jq -S` flag sorts object keys, not array elements, so the array order in the output must match the expected file literally — which it does.

**Check 10 — Evidence claim about `expected.json` location.**
The evidence says `expected.json` is at `tests/fixtures/list-archived-excluded/recorded-responses/expected.json`, but the actual file is at `tests/fixtures/list-archived-excluded/expected.json` (one directory up). The bats test at line 151 references `"$FIXTURES/list-archived-excluded/expected.json"` — the correct path. This is a minor inaccuracy in the evidence comment's path description only; the code and file are consistent.

---

## Findings

**No bugs or logic errors found.**

One minor evidence inaccuracy:

- **Location:** Issue file `/repo/.features/github-issues-binding/issues/20-tracker-snapshot-list-orderby-fix.md`, evidence block item 4.
- **Problem:** The evidence states "Fixture now has `#7` first, `#5` (CLOSED) second, `#3` third. `expected.json` unchanged (`["alpha-feature","beta-feature"]`)." — this is accurate about the fixture and `expected.json` content. However, the evidence block says `tests/fixtures/list-archived-excluded/recorded-responses/expected.json` does not exist at that path. The actual `expected.json` is at `tests/fixtures/list-archived-excluded/expected.json`. The bats test itself correctly references `"$FIXTURES/list-archived-excluded/expected.json"`, so this has no functional impact.
- **Severity:** 🟢 Minor — this is a description in the implementation comment, not a code defect. The test uses the correct path.

**No other findings.** All seven acceptance criteria are satisfied, the sub-issue path is untouched, and the fixture now constitutes a genuine proof of the in-script sort — the test would fail without the `sort_by(.number)` because the fixture delivers `beta-feature` before `alpha-feature` but the expected output requires `alpha-feature` first.

**Pending human verification:** None — all criteria have machine-checkable proofs in the test suite.
