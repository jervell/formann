---
status: ready-for-agent
category: enhancement
type: AFK
---

# Cleanup bundle: ref-shape language + binding-script hygiene

> *This was generated by AI during to-issues.*

## Parent

`.features/github-issues-binding/PRD.md`

## What to build

Four small fixes, each tiny in isolation but worth bundling so they ride one review pass:

1. **`framework/runner/review-and-gate.md:7`** — Replace *"It looks like `<feature>/<NN>` (canonical form)."* with binding-neutral language. The runner appends the binding-native ref; under github-issues that's `#42`, not `<feature>/<NN>`. Point at `docs/formann/issue-tracker/BINDING.md` for the per-binding ref shape.

2. **`framework/bindings/issue-tracker/github-issues/tracker-snapshot:220`** — The 0-match path interpolates `$_slug` into JSON via `printf '%s'`, then pipes to `jq -c '.'`. Use the same `$(printf '%s' "$_slug" | jq -Rs '.')` pattern that line 322 already uses for proper JSON encoding. Defence against a slug containing `"` or `\` — unreachable today given the slug grammar, but the two paths should not diverge.

3. **`framework/bindings/issue-tracker/github-issues/body-edit`** — The awk script compares `$0 == heading` for exact match. The local-markdown `tracker-snapshot` explicitly strips `\r` before comparison and ships a CRLF regression test (`tracker-snapshot.bats:205`). `body-edit` doesn't. Strip `\r` from each input line so a CRLF-encoded body's `## Agent Brief\r` heading still matches.

4. **`framework/runner/run-the-queue.sh:1207`** (`collect_binding_env`) — On a malformed-line error, the message echoes the entire offending line: `echo "runner: sandbox-env emitted malformed line (...): $line"`. If a future binding's `sandbox-env` ever emits a typo'd `gH_TOKEN=ghp_…` (uppercase-key regex fails), the token reaches stderr, terminal, and `runner.log`. Echo only the key portion (`${line%%=*}`) or a fixed message, never the value.

## Acceptance criteria

- [ ] `review-and-gate.md` no longer hard-codes the `<feature>/<NN>` ref shape; the wording is binding-neutral and points at the binding doc.
- [ ] `tracker-snapshot`'s 0-match path JSON-escapes the slug the same way the 1-match path does.
- [ ] `body-edit` handles CRLF-encoded issue bodies (heading match still works when input lines end in `\r`); a bats regression test covers it.
- [ ] `collect_binding_env`'s malformed-line error message never includes the post-`=` portion of the offending line; a bats regression test asserts this for a `KEY=secret-payload` line.
- [ ] All existing tests continue to pass.

## Blocked by

- `github-issues-binding/15`

## Agent Brief

> *This was generated by AI during triage.*

**Summary:** Four small fixes bundled into one review pass. Three are codebase-fit hygiene; one is a secret-leak hardening. They're unrelated in mechanism but each small enough that bundling earns one review instead of four.

**Current behavior:**

1. **`framework/runner/review-and-gate.md`** — A line of the runner prompt names the canonical ref form as `<feature>/<NN>` and says "(canonical form)". This is local-markdown-specific; under the github-issues binding the appended ref is `#N`, not `<feature>/<NN>`.
2. **`tracker-snapshot` (GH binding), 0-match path** — When `_parent_count == 0`, the script interpolates `$_slug` into JSON via raw `printf '%s'`. The 1-match / `≥2`-match paths already use the `printf '%s' "$_slug" | jq -Rs '.'` pattern. The two paths diverge; a slug containing `"` or `\` would produce malformed JSON on the 0-match path (unreachable today given the slug grammar, but defensible to align).
3. **`body-edit` (GH binding)** — The awk script compares input lines to the heading via `$0 == heading` (exact match). The local-markdown `tracker-snapshot` strips `\r` from each line before comparison (with a CRLF-regression bats test). `body-edit` doesn't; a CRLF-encoded issue body's heading `## Agent Brief\r` fails to match the literal `## Agent Brief`.
4. **`collect_binding_env` in `run-the-queue.sh`** — On a malformed-line error, the message echoes the full offending line: `... malformed line (...): $line`. If a future binding's `sandbox-env` emits a typo'd uppercase-key form like `gH_TOKEN=ghp_…`, the regex fails and the token reaches stderr, the terminal, and `runner.log`.

**Desired behavior:**

1. The `review-and-gate.md` line is binding-neutral: it describes the ref as "the binding-native ref form" and points at `docs/formann/issue-tracker/BINDING.md` for the per-binding shape.
2. The 0-match path JSON-escapes the slug the same way the 1-match / `≥2`-match paths do.
3. `body-edit`'s awk script strips `\r` from each input line before comparison; a bats regression test asserts a CRLF-encoded body's heading still matches.
4. `collect_binding_env`'s malformed-line error message includes only the key portion (`${line%%=*}`) — or a fixed message — and never the post-`=` portion. A bats regression test asserts a `KEY=secret-payload`-shaped line's secret-payload does not appear in the error output.

**Key interfaces:**

- `framework/runner/review-and-gate.md` — the prompt text naming the ref shape.
- `framework/bindings/issue-tracker/github-issues/tracker-snapshot` — the 0-match-parent error path. Use the same `printf '%s' ... | jq -Rs '.'` pattern the other paths already use.
- `framework/bindings/issue-tracker/github-issues/body-edit` — the awk script's heading-match step. Strip `\r` from `$0` before comparison.
- `collect_binding_env` in `framework/runner/run-the-queue.sh` — the malformed-line stderr message.

**Acceptance criteria:**

- [ ] `review-and-gate.md` no longer hard-codes the `<feature>/<NN>` ref shape; the wording is binding-neutral and points at the binding doc.
- [ ] `tracker-snapshot`'s 0-match path JSON-escapes the slug the same way the 1-match path does.
- [ ] `body-edit` handles CRLF-encoded issue bodies (heading match still works when input lines end in `\r`); a bats regression test covers it.
- [ ] `collect_binding_env`'s malformed-line error message never includes the post-`=` portion of the offending line; a bats regression test asserts this for a `KEY=secret-payload`-shaped line.
- [ ] All existing tests continue to pass.

**Out of scope:**

- Refactoring the dispatch of the malformed-line error beyond what's needed to redact the value.
- Reworking `body-edit`'s overall awk structure beyond the `\r` strip.
