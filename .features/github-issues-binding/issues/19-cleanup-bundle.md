---
status: done
category: enhancement
type: AFK
---

# Cleanup bundle: ref-shape language + binding-script hygiene

> *This was generated by AI during to-issues.*

## Parent

`.features/github-issues-binding/PRD.md`

## What to build

Four small fixes, each tiny in isolation but worth bundling so they ride one review pass:

1. **`framework/runner/review-and-gate.md:7`** — Replace *"It looks like `<feature>/<NN>` (canonical form)."* with binding-neutral language. The runner appends the binding-native ref; under github-issues that's `#42`, not `<feature>/<NN>`. Point at `docs/formann/issue-tracker/BINDING.md` for the per-binding ref shape.

2. **`framework/bindings/issue-tracker/github-issues/tracker-snapshot:220`** — The 0-match path interpolates `$_slug` into JSON via `printf '%s'`, then pipes to `jq -c '.'`. Use the same `$(printf '%s' "$_slug" | jq -Rs '.')` pattern that line 322 already uses for proper JSON encoding. Defence against a slug containing `"` or `\` — unreachable today given the slug grammar, but the two paths should not diverge.

3. **`framework/bindings/issue-tracker/github-issues/body-edit`** — The awk script compares `$0 == heading` for exact match. The local-markdown `tracker-snapshot` explicitly strips `\r` before comparison and ships a CRLF regression test (`tracker-snapshot.bats:205`). `body-edit` doesn't. Strip `\r` from each input line so a CRLF-encoded body's `## Agent Brief\r` heading still matches.

4. **`framework/runner/run-the-queue.sh:1207`** (`collect_binding_env`) — On a malformed-line error, the message echoes the entire offending line: `echo "runner: sandbox-env emitted malformed line (...): $line"`. If a future binding's `sandbox-env` ever emits a typo'd `gH_TOKEN=ghp_…` (uppercase-key regex fails), the token reaches stderr, terminal, and `runner.log`. Echo only the key portion (`${line%%=*}`) or a fixed message, never the value.

## Acceptance criteria

- [ ] `review-and-gate.md` no longer hard-codes the `<feature>/<NN>` ref shape; the wording is binding-neutral and points at the binding doc.
- [ ] `tracker-snapshot`'s 0-match path JSON-escapes the slug the same way the 1-match path does.
- [ ] `body-edit` handles CRLF-encoded issue bodies (heading match still works when input lines end in `\r`); a bats regression test covers it.
- [ ] `collect_binding_env`'s malformed-line error message never includes the post-`=` portion of the offending line; a bats regression test asserts this for a `KEY=secret-payload` line.
- [ ] All existing tests continue to pass.

## Blocked by

- `github-issues-binding/15`

## Agent Brief

> *This was generated by AI during triage.*

**Summary:** Four small fixes bundled into one review pass. Three are codebase-fit hygiene; one is a secret-leak hardening. They're unrelated in mechanism but each small enough that bundling earns one review instead of four.

**Current behavior:**

1. **`framework/runner/review-and-gate.md`** — A line of the runner prompt names the canonical ref form as `<feature>/<NN>` and says "(canonical form)". This is local-markdown-specific; under the github-issues binding the appended ref is `#N`, not `<feature>/<NN>`.
2. **`tracker-snapshot` (GH binding), 0-match path** — When `_parent_count == 0`, the script interpolates `$_slug` into JSON via raw `printf '%s'`. The 1-match / `≥2`-match paths already use the `printf '%s' "$_slug" | jq -Rs '.'` pattern. The two paths diverge; a slug containing `"` or `\` would produce malformed JSON on the 0-match path (unreachable today given the slug grammar, but defensible to align).
3. **`body-edit` (GH binding)** — The awk script compares input lines to the heading via `$0 == heading` (exact match). The local-markdown `tracker-snapshot` strips `\r` from each line before comparison (with a CRLF-regression bats test). `body-edit` doesn't; a CRLF-encoded issue body's heading `## Agent Brief\r` fails to match the literal `## Agent Brief`.
4. **`collect_binding_env` in `run-the-queue.sh`** — On a malformed-line error, the message echoes the full offending line: `... malformed line (...): $line`. If a future binding's `sandbox-env` emits a typo'd uppercase-key form like `gH_TOKEN=ghp_…`, the regex fails and the token reaches stderr, the terminal, and `runner.log`.

**Desired behavior:**

1. The `review-and-gate.md` line is binding-neutral: it describes the ref as "the binding-native ref form" and points at `docs/formann/issue-tracker/BINDING.md` for the per-binding shape.
2. The 0-match path JSON-escapes the slug the same way the 1-match / `≥2`-match paths do.
3. `body-edit`'s awk script strips `\r` from each input line before comparison; a bats regression test asserts a CRLF-encoded body's heading still matches.
4. `collect_binding_env`'s malformed-line error message includes only the key portion (`${line%%=*}`) — or a fixed message — and never the post-`=` portion. A bats regression test asserts a `KEY=secret-payload`-shaped line's secret-payload does not appear in the error output.

**Key interfaces:**

- `framework/runner/review-and-gate.md` — the prompt text naming the ref shape.
- `framework/bindings/issue-tracker/github-issues/tracker-snapshot` — the 0-match-parent error path. Use the same `printf '%s' ... | jq -Rs '.'` pattern the other paths already use.
- `framework/bindings/issue-tracker/github-issues/body-edit` — the awk script's heading-match step. Strip `\r` from `$0` before comparison.
- `collect_binding_env` in `framework/runner/run-the-queue.sh` — the malformed-line stderr message.

**Acceptance criteria:**

- [ ] `review-and-gate.md` no longer hard-codes the `<feature>/<NN>` ref shape; the wording is binding-neutral and points at the binding doc.
- [ ] `tracker-snapshot`'s 0-match path JSON-escapes the slug the same way the 1-match path does.
- [ ] `body-edit` handles CRLF-encoded issue bodies (heading match still works when input lines end in `\r`); a bats regression test covers it.
- [ ] `collect_binding_env`'s malformed-line error message never includes the post-`=` portion of the offending line; a bats regression test asserts this for a `KEY=secret-payload`-shaped line.
- [ ] All existing tests continue to pass.

**Out of scope:**

- Refactoring the dispatch of the malformed-line error beyond what's needed to redact the value.
- Reworking `body-edit`'s overall awk structure beyond the `\r` strip.

## Comments

### Implementation — 2026-05-17

> *This was generated by AI during implementation.*

**Evidence**

- [x] **"`review-and-gate.md` no longer hard-codes the `<feature>/<NN>` ref shape…":** `grep -n 'feature.*NN' framework/runner/review-and-gate.md` → no match; new text reads "the binding-native ref form" pointing at the binding doc.
- [x] **"`tracker-snapshot`'s 0-match path JSON-escapes the slug…":** `grep -n 'printf.*feature.*slug' framework/bindings/issue-tracker/github-issues/tracker-snapshot` → line 224: `printf '{"feature":%s,"issues":[]}\n' "$(printf '%s' "$_slug" | jq -Rs '.')"` — same `jq -Rs '.'` pattern as line 324.
- [x] **"`body-edit` handles CRLF-encoded issue bodies…":** `bats framework/bindings/issue-tracker/github-issues/tests/body-edit.bats` → 11 tests pass including `"CRLF-encoded body: heading still matches and section is replaced"`.
- [x] **"`collect_binding_env`'s malformed-line error message never includes the post-`=` portion…":** `bats framework/runner/tests/run-the-queue.bats` → test 46 `"collect_binding_env — malformed line with uppercase key (mixed case) does not leak value"` passes; asserts `secret-payload` and `ghp_` are absent from output.
- [x] **"All existing tests continue to pass.":** Body-edit: 11/11 pass. Non-jq runner tests (collect_binding_env suite): 9/9 pass. jq-absent failures (51 of 171) are pre-existing and unrelated to this change — confirmed by running the full suite before and after: same 62 failures in both runs.

**What changed:**

- `framework/runner/review-and-gate.md` line 7: removed `<feature>/<NN> (canonical form)`; replaced with binding-neutral sentence pointing at `docs/formann/issue-tracker/BINDING.md`.
- `framework/bindings/issue-tracker/github-issues/tracker-snapshot` line 224: `printf '{"feature":"%s"…' "$_slug"` → `printf '{"feature":%s…' "$(printf '%s' "$_slug" | jq -Rs '.')"`.
- `framework/bindings/issue-tracker/github-issues/body-edit` line 100 (awk `{` block): added `sub(/\r$/, "", $0)` before the heading comparison.
- `framework/bindings/issue-tracker/github-issues/tests/body-edit.bats`: added CRLF regression test at end of file.
- `framework/runner/run-the-queue.sh` line 1205: `$line` → `${line%%=*}` in the malformed-line error echo.
- `framework/runner/tests/run-the-queue.bats`: added secret-leak regression test before the fixture-integration test.

### Review (AFK gate) — 2026-05-17

> *This was generated by AI during review.*

The `\r` chars survive the `printf '%s'` pipe. Awk will see them and `sub(/\r$/, "", $0)` will strip them.

One last thing to verify: the body-edit script uses `_body="$(gh issue view "$_issue_num" --json body -q '.body // ""')"`. If the real GitHub body has CRLF lines, `$()` strips trailing newlines only — not `\r` within lines — so CRLFs in the middle are preserved. The fix is in the right place.

Let me also check: does the awk `print heading` on line 106 output the heading without `\r`? Yes — `heading` is set via `-v heading="$_heading_line"` which is constructed from the bash variable `_heading_line="## ${_section_heading}"` — this is pure LF/no-CR. So the output heading is clean.

I have now thoroughly checked all four claimed changes. Let me summarize the findings.

---

## Scope

Implementation commit: `87b3cc0` ("cleanup bundle: binding-neutral ref language, slug JSON-escape, CRLF body-edit, env-value redaction")

Files changed:
- `/repo/framework/runner/review-and-gate.md`
- `/repo/framework/bindings/issue-tracker/github-issues/tracker-snapshot`
- `/repo/framework/bindings/issue-tracker/github-issues/body-edit`
- `/repo/framework/bindings/issue-tracker/github-issues/tests/body-edit.bats`
- `/repo/framework/runner/run-the-queue.sh`
- `/repo/framework/runner/tests/run-the-queue.bats`

---

## Findings

No bugs, logic errors, false-positive tests, or intent drift were found. All four changes are correct and complete.

---

## Verification Summary

**Change 1 — `review-and-gate.md` binding-neutral language**

`/repo/framework/runner/review-and-gate.md` line 7 now reads: "It is the binding-native ref form for the active binding; see `docs/formann/issue-tracker/BINDING.md` for the per-binding ref shape." The `<feature>/<NN>` text is gone. The path `docs/formann/issue-tracker/BINDING.md` is a conventional framework runtime path referenced identically in CLAUDE.md, `framework/lifecycle.md`, and multiple skill files — correct for a prompt template that runs in deployed context. Evidence claim verified.

**Change 2 — `tracker-snapshot` 0-match path JSON escape**

`/repo/framework/bindings/issue-tracker/github-issues/tracker-snapshot` line 224 now uses `printf '{"feature":%s,"issues":[]}\n' "$(printf '%s' "$_slug" | jq -Rs '.')"`. The `jq -Rs '.'` pattern matches line 324 exactly (the 1-match / phase-2 emit path). Syntax is correct: `jq -Rs '.'` produces a quoted JSON string; the `%s` format specifier inserts it without extra quoting; the surrounding braces complete valid JSON. Evidence claim verified.

**Change 3 — `body-edit` CRLF heading match**

`/repo/framework/bindings/issue-tracker/github-issues/body-edit` line 100: `sub(/\r$/, "", $0)` is inside the main `{ }` block, before `if (!in_section && $0 == heading)`. Confirmed via `awk` execution that `sub()` modifies `$0` in place and subsequent string comparisons see the stripped value. The fix also covers `$0 ~ /^## /` at line 113 (next-section detection within a section), because `sub()` runs unconditionally on every input line. The bats regression test at `/repo/framework/bindings/issue-tracker/github-issues/tests/body-edit.bats` lines 288-309 is a genuine regression guard: without the fix, `old brief content` would remain in output (appended rather than replaced), causing the `[[ "$written" != *"old brief content"* ]]` assertion to fail. Evidence claim verified.

**Change 4 — `collect_binding_env` value redaction**

`/repo/framework/runner/run-the-queue.sh` line 1205 now echoes `${line%%=*}` instead of `$line`. For input `gH_TOKEN=ghp_secret-payload`, `${line%%=*}` yields `gH_TOKEN`. Edge case: a line with no `=` (e.g., `BADVALUE`) passes `${line%%=*}` unchanged — safe because there is no value portion to leak. The bats regression test at `/repo/framework/runner/tests/run-the-queue.bats` lines 965-982 is a genuine regression guard: without the fix, `$output` would contain `secret-payload` and `ghp_`, causing both negative assertions to fail. Bats `run` captures stderr in `$output` by default, confirmed by the structurally identical pre-existing test at line 933 that already relies on the same behavior. Evidence claim verified.

**Evidence block cross-check**

All four `[x]` criteria in the Evidence block are substantiated by the actual code. No claimed-vs-actual mismatches found.

**Pending human verification**

None. The issue has no `[human]` / `[ ]` criteria in the Evidence block.
