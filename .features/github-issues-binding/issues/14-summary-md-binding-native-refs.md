---
status: in-review
category: enhancement
type: AFK
---

# Fix SUMMARY.md ref handling for binding-native refs

> *This was generated by AI during to-issues.*

## Parent

`.features/github-issues-binding/PRD.md`

## What to build

Three SUMMARY-formatting sites in `framework/runner/run-the-queue.sh` regex-split refs on `/`, which breaks under the github-issues binding (refs of shape `#42`):

- **`format_summary_md`** (≈lines 358-369): `slash = index(ref, "/")` — for `#42`, `slash == 0`, so `nn = ref = "#42"` and the link becomes `[#42.log](#42.log)` while the actual file on disk is `42.log`. Every single-feature SUMMARY emits broken links under GH.
- **`print_multi_feature_records`** (≈lines 617-634): filters dispatch records via `[[ "$d_ref" == "$feature"/* ]]`. GH refs never contain `/`, so every drained GH-binding feature emits a table with zero `I` rows.
- **`format_multi_feature_summary_md`** (≈line 434): uses `ref` verbatim as the log path; under GH this is doubly wrong (file lives at `<feature>/42.log`; the link points at `#42.log`).

Refactor the formatters to consume binding-agnostic `nn` and `feature` fields the dispatch path already has in scope. The simplest path is to widen the `RUN_DISPATCHES` record schema from `ref|outcome|duration|review_present` to `feature|nn|ref|outcome|duration|review_present` so the formatters never have to recover `nn` from the ref string. Plumb `feature` and `nn` through `record_dispatch` and the existing call sites in `dispatch_one`.

## Acceptance criteria

- [ ] Single-feature SUMMARY.md emits a working `[<nn>.log](<nn>.log)` link under both bindings (verified with a fixture exercising a GH-shaped ref).
- [ ] Multi-feature drain SUMMARY.md emits the expected `I` rows per drained feature regardless of ref shape (GH-binding features no longer render empty tables).
- [ ] All `record_dispatch` call sites in `dispatch_one` are updated together — no half-refactored call.
- [ ] Existing local-markdown SUMMARY rendering is byte-identical for `<feature>/NN`-shaped refs (regression coverage).
- [ ] Bats coverage in `framework/runner/tests/run-the-queue.bats` exercises both `format_summary_md` and `format_multi_feature_summary_md` with `#N`-shaped refs.

## Blocked by

None — can start immediately.

## Agent Brief

> *This was generated by AI during triage.*

**Summary:** Three SUMMARY-formatting sites in `run-the-queue.sh` split refs on `/`, silently breaking under the github-issues binding (refs of shape `#N` carry no `/`). Widen the dispatch record schema so the formatters consume binding-agnostic `nn` and `feature` fields directly, instead of trying to recover them from the ref string.

**Current behavior:**
The runner stores per-issue outcomes in the `RUN_DISPATCHES` array as `ref|outcome|duration|review_present`. SUMMARY formatters recover `nn` from the ref by indexing on `/`:

- `format_summary_md` derives `nn = substr(ref, slash+1)` — for a GH ref `#42`, `slash == 0` and `nn` falls back to the whole ref `#42`, producing the broken link `[#42.log](#42.log)` while the actual log file is `42.log`.
- `print_multi_feature_records` filters dispatch rows using `[[ "$d_ref" == "$feature"/* ]]`. GH refs never contain `/`, so the predicate never matches; every drained GH-binding feature emits a table with zero `I` rows.
- `format_multi_feature_summary_md` uses raw `ref` as the log basename — wrong under GH on both axes (file lives at `<feature>/42.log`; the link points at `#42.log`).

Local-markdown's `<feature>/NN` shape happens to satisfy both predicates, masking the breakage entirely under the legacy binding.

**Desired behavior:**
The dispatch record schema carries `feature` and `nn` alongside `ref` so formatters never reparse the ref string. Single-feature and multi-feature SUMMARY rendering produces correct links and populated tables under both bindings. Output under local-markdown's `<feature>/NN` refs is byte-identical to today's.

**Key interfaces:**

- `RUN_DISPATCHES` record schema — widen so every formatter has `feature` and `nn` available without parsing `ref`. All `record_dispatch` call sites in `dispatch_one` updated together; no half-refactored call site.
- `format_summary_md` — composes link basenames from `nn`.
- `print_multi_feature_records` — filters by `feature` field equality, not ref-prefix matching.
- `format_multi_feature_summary_md` — composes per-feature log paths from `feature` and `nn`.

**Acceptance criteria:**

- [ ] Single-feature SUMMARY emits `[<nn>.log](<nn>.log)` correctly under both bindings.
- [ ] Multi-feature drain SUMMARY emits the expected `I` rows per drained feature regardless of ref shape.
- [ ] All `record_dispatch` call sites in `dispatch_one` carry the widened fields.
- [ ] Local-markdown SUMMARY rendering is byte-identical for `<feature>/NN`-shaped refs (no regression).
- [ ] Bats coverage in `framework/runner/tests/run-the-queue.bats` exercises both `format_summary_md` and `format_multi_feature_summary_md` with `#N`-shaped refs.

**Out of scope:**

- Changing binding-native ref shapes (`#N` for GH, `<feature>/NN` for local-markdown).
- Reformatting SUMMARY.md output structure beyond what's necessary to make links and tables correct.

## Comments

### Implementation — 2026-05-17

> *This was generated by AI during implementation.*

**Evidence**

- [x] **"Single-feature SUMMARY.md emits a working `[<nn>.log](<nn>.log)` link…":** `format_summary_md — GH-shaped ref (#N) emits working nn-based log link` (test 82); build green (170/170 pass).
- [x] **"Multi-feature drain SUMMARY.md emits the expected `I` rows per drained feature…":** `format_multi_feature_summary_md — GH-shaped ref (#N) emits feature-path log link` (test 137); `print_multi_feature_records` filter changed from `[[ "$d_ref" == "$feature"/* ]]` to `[ "$d_feature" = "$feature" ]`; build green.
- [x] **"All `record_dispatch` call sites in `dispatch_one` are updated together…":** `grep -c 'record_dispatch "\$feature" "\$nn"' framework/runner/run-the-queue.sh` → 10 call sites, all carrying the widened args; verified by inspection and test coverage.
- [x] **"Existing local-markdown SUMMARY rendering is byte-identical for `<feature>/NN`-shaped refs…":** `format_summary_md — heading, run line, stop reason, per-issue table with logs` and `finalize_run — writes SUMMARY.md with feature, run line…` both pass with local-markdown input; output confirmed `| f/01 | in-review | 42s | [01.log](01.log) |`.
- [x] **"Bats coverage in `framework/runner/tests/run-the-queue.bats` exercises both `format_summary_md` and `format_multi_feature_summary_md` with `#N`-shaped refs…":** Two new tests added — test 82 (`format_summary_md — GH-shaped ref (#N) emits working nn-based log link`) and test 137 (`format_multi_feature_summary_md — GH-shaped ref (#N) emits feature-path log link`); both pass.

**What changed:**

- `RUN_DISPATCHES` record schema widened from `ref|outcome|duration|review` to `feature|nn|ref|outcome|duration|review`.
- `record_dispatch` signature changed to `(feature, nn, ref, label, duration, review)`.
- `format_summary_md` awk: reads `nn` from field 2 instead of deriving it by slash-splitting `ref`. Log link is `[<nn>.log](<nn>.log)`; issue column still shows the binding-native `ref`.
- `print_multi_feature_records`: filter changed from `[[ "$d_ref" == "$feature"/* ]]` to `[ "$d_feature" = "$feature" ]` (exact equality on the new `feature` field).
- `format_multi_feature_summary_md` awk I-row handler: reads `feature` and `nn` from dedicated fields; composes log path as `feature/nn.log` so per-run artifacts don't collide across features regardless of ref shape.
- All 10 `record_dispatch` call sites in `dispatch_one` carry the widened `feature` and `nn` arguments.

### Review (AFK gate) — 2026-05-17

> *This was generated by AI during review.*

**Scope resolved:**

Commit: `8358b1c` ("runner: widen RUN_DISPATCHES schema for binding-native ref support (#14)")  
Files: `/repo/framework/runner/run-the-queue.sh`, `/repo/framework/runner/tests/run-the-queue.bats`

---

## Findings

### Finding 1: `format_end_of_run_table` was not updated to the new schema — the end-of-run stdout table is broken

**Location:** `/repo/framework/runner/run-the-queue.sh`, lines 326-344

**Problem:** `format_end_of_run_table` reads `$1`, `$2`, `$3` as issue/outcome/duration respectively. In the old schema these were `ref|outcome|duration`. After the schema widening they are `feature|nn|ref`. The function is fed by `print_dispatch_records` (which emits the new `feature|nn|ref|outcome|duration|review` format) via `finalize_run` at line 658. As a result, the operator's end-of-run table — printed to stdout at the end of every run — now renders `feature` in the "issue" column, `nn` in the "outcome" column, and `ref` with a spurious `s` suffix appended in the "duration" column.

For a local-markdown run of feature `f`, issues `01` and `02`, the current output would be:

```
issue  outcome  duration
f      01       f/01s
f      02       f/02s
stop reason: queue-empty
```

instead of:

```
issue   outcome    duration
f/01    in-review  42s
f/02    FAIL       18s
stop reason: queue-empty
```

This is a fourth formatter that consumes the same `RUN_DISPATCHES` stream and was broken by the schema change. It was not listed in the issue's original three-site enumeration (because it had no slash-splitting bug of its own), but the schema widening is its collateral damage.

**Severity:** 🔴 Critical — the end-of-run table is the operator's primary at-a-glance view of what happened in a run. It is emitted on stdout (and mirrored to `runner.log`) at the end of every single-feature and single-issue run.

---

### Finding 2: Unit test for `format_end_of_run_table` still uses the old record schema — the test no longer tests the production data path

**Location:** `/repo/framework/runner/tests/run-the-queue.bats`, lines 1625-1639

**Problem:** The test feeds `format_end_of_run_table` with old-schema records (`afk-runner/01|in-review|42`) directly, bypassing `print_dispatch_records`. Because `format_end_of_run_table` was not updated, the test continues to pass. But it no longer exercises the function in the way production uses it. The integration test at line 1925 ("run_loop + finalize_run") does exercise the real path but only asserts `--partial "f/01"`, which passes because `f/01` appears in the wrong "duration" column as `f/01s`.

**Severity:** 🟡 Important — the test gives a false green signal. The broken behavior is not caught.

---

### Finding 3: Three stale schema comments in `run-the-queue.sh`

**Location 1:** `/repo/framework/runner/run-the-queue.sh`, line 347  
Line says: `Reads ref|outcome|duration|review_present records`  
Actual: `feature|nn|ref|outcome|duration|review_present`

**Location 2:** `/repo/framework/runner/run-the-queue.sh`, line 381  
Line says: `I|<ref>|<outcome>|<duration>|<review_present>`  
Actual: `I|<feature>|<nn>|<ref>|<outcome>|<duration>|<review_present>`

**Location 3:** `/repo/framework/runner/run-the-queue.sh`, line 504  
Line says: `RUN_DISPATCHES is an indexed array of feature|nn|ref|outcome|duration strings`  
Actual: `feature|nn|ref|outcome|duration|review_present` — the `review_present` field is missing.

These comments describe the old schema. Anyone writing a new caller or formatter against this documentation will produce broken code.

**Severity:** 🟢 Minor (stale documentation comments — codebase-fit violation)

---

## Verification summary

**Checked:**

- All 10 `record_dispatch` call sites in `dispatch_one` — all carry widened `feature`, `nn`, `ref` args. AC criterion 3: verified correct.
- `format_summary_md` awk field assignments — correctly reads `feature=$1; nn=$2; ref=$3; out=$4; dur=$5`. AC criterion 1: verified correct.
- `print_multi_feature_records` filter — correctly changed to `[ "$d_feature" = "$feature" ]`. AC criterion 2: verified correct.
- `format_multi_feature_summary_md` I-row handler — correctly reads `feature=$2; nn=$3; ref=$4; out=$5; dur=$6`. AC criterion 2: verified correct.
- New bats test `format_summary_md — GH-shaped ref (#N) emits working nn-based log link` — correctly constructed, exercises GH-shaped ref. AC criterion 5: verified correct.
- New bats test `format_multi_feature_summary_md — GH-shaped ref (#N) emits feature-path log link` — correctly constructed, exercises GH-shaped ref and review log path. AC criterion 5: verified correct.
- Existing local-markdown tests updated to new schema — verified the test inputs match the widened schema and expected output is unchanged. AC criterion 4: verified correct for the SUMMARY.md path.
- `format_end_of_run_table` — **NOT updated**. Consumes `print_dispatch_records` output, which now has 6 fields; function still reads `$1/$2/$3` as issue/outcome/duration. This is a regression introduced by the schema widening. Not covered in the acceptance criteria (the issue's three named sites did not include this function), but it is a real breakage.
- Evidence block claim "10 call sites" — confirmed by grep, all carry widened args.
- Evidence block claims about test numbers (82, 137) — not verified (test numbering not stable in bats). The tests themselves were verified by content and logic.

**Pending human verification:**

None of the acceptance criteria is marked `[human]` in the issue file.

### Implementation (2) — 2026-05-17

> *This was generated by AI during implementation.*

Rework addressing the review's Critical and Important findings.

**Evidence**

- [x] **"Single-feature SUMMARY.md emits a working `[<nn>.log](<nn>.log)` link…":** unchanged from prior implementation; covered by `format_summary_md — GH-shaped ref (#N) emits working nn-based log link`; bats 172/172 green.
- [x] **"Multi-feature drain SUMMARY.md emits the expected `I` rows per drained feature…":** unchanged; covered by `format_multi_feature_summary_md — GH-shaped ref (#N) emits feature-path log link`; bats 172/172 green.
- [x] **"All `record_dispatch` call sites in `dispatch_one` are updated together…":** unchanged; `grep -c 'record_dispatch "\$feature" "\$nn"' framework/runner/run-the-queue.sh` → 10 sites.
- [x] **"Existing local-markdown SUMMARY rendering is byte-identical for `<feature>/NN`-shaped refs…":** unchanged for SUMMARY.md; additionally `format_end_of_run_table` now renders `f/01  in-review  42s` under local-markdown input (verified by tightened unit test feeding new-schema records with row-anchored regex `^afk-runner/01 .*in-review.* 42s *$`).
- [x] **"Bats coverage in `framework/runner/tests/run-the-queue.bats` exercises both `format_summary_md` and `format_multi_feature_summary_md` with `#N`-shaped refs…":** unchanged for those two; additionally `format_end_of_run_table — GH-shaped ref (#N) renders binding-native issue column` exercises `#42` / `#43` refs end-to-end.

**Findings addressed**

- 🔴 *Critical (Finding 1) — `format_end_of_run_table` wasn't rewired to the new schema.* Confirmed by sourcing the script and feeding new-schema records: the broken function emitted `f / 01 / f/01s` in the issue/outcome/duration columns. Fixed: awk now reads `$3, $4, $5` as ref/outcome/duration (matching `format_summary_md`). The function's docstring is updated to describe the 6-field input and the rationale for dropping `feature` and `nn` from the operator view.
- 🟡 *Important (Finding 2) — unit test bypassed `print_dispatch_records` and exercised the stale schema.* Confirmed: the prior input `afk-runner/01|in-review|42` is a 3-field record that the broken function happened to render correctly; the new schema produces 6 fields. Fixed: the test now feeds the same `feature|nn|ref|outcome|duration|review` shape `print_dispatch_records` emits, with row-anchored regexes (`^afk-runner/01 .*in-review.* 42s *$`) plus a negative assertion (`! ... '^afk-runner +01 '`) that fires if `feature`/`nn` ever leak back into the operator-facing columns. Re-replayed both tests against a buggy variant of the function and confirmed each assertion now fails (load-bearing).
- 🟡 *Important (Finding 2, integration test) — `run_loop + finalize_run` asserted `--partial "f/01"`, which incidentally matched the malformed `f/01s` duration column.* Fixed: tightened to `assert_output --regexp 'f/01 +in-review +1s'`, which anchors `ref`, `outcome`, and `duration` to their respective columns. Replay confirms the new assertion fails against the broken function.
- 🟢 *Minor (Finding 3) — three stale schema comments at lines 347, 381, 504.* The docstring at line 347 (now ~322) belonged to the function being fixed, so it was updated as part of this rework. The other two were explicitly out of scope per the maintainer's framing (Critical + Important only); leaving them to follow-up.

**Decisions made**

- Kept the `issue` column showing the binding-native `ref` (not `feature`+`nn`). That matches the prior operator-facing semantics under local-markdown (`f/01`) and gives a one-symbol identity under github-issues (`#42`).
- Tightened the unit-test regex pattern (`^… .* … .* … *$`) over a looser `grep -q '…'` because the latter is what let the bug ride for the first cycle.

**Adversarial review**

Spawned an adversarial reviewer to attack the fix. Result: clean across all six dimensions checked — fix works under both bindings, every modified/added test is load-bearing (fails against the pre-fix function), no other awk consumer of `RUN_DISPATCHES` / `print_dispatch_records` was missed, empty/malformed input still handled, docstring accurate. Bats: 172/172 pass.
