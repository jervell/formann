---
status: in-review
category: bug
type: AFK
---

# Strengthen `list-archived-excluded` to actually demonstrate exclusion

> *This was generated by AI during to-issues.*

## Parent

`.features/github-issues-binding/PRD.md`

## What to build

The fixture at `framework/bindings/issue-tracker/github-issues/tests/fixtures/list-archived-excluded/recorded-responses/graphql-response.json` contains only OPEN parents (`#3 alpha-feature`, `#7 beta-feature`). The test `--list: archived (closed) parent absent — only open parents returned` therefore proves only that two open features survive — the trivial open-only baseline. The promised exclusion behaviour relies entirely on the GraphQL `states: [OPEN]` clause and is not exercised by any test.

Pick one of:

- **(a) Strengthen the script-level filter.** Augment the fixture to include a CLOSED `formann:feature`-labelled parent in the recorded response. The `gh` shim serves the response verbatim regardless of the query, so to make the test meaningful the `tracker-snapshot` script must explicitly drop closed entries (or assert at the script level that closed entries don't make it into the output). Update `expected.json` to assert the closed parent is filtered out.
- **(b) Assert the query string directly.** Have the `gh` shim record the full query string, and add a separate test that grep's the recorded query for `states: [OPEN]`. Pair this with a fixture covering deduplication only, and rename the existing test so its name matches what it verifies.

Either way: no test in `tracker-snapshot.bats` should claim to verify archived-parent exclusion without actually exercising it, and the `states: [OPEN]` contract must have at least one direct check.

## Acceptance criteria

- [ ] No test in `tracker-snapshot.bats` claims to verify archived-parent exclusion without actually exercising it (test names match verified behaviour).
- [ ] The GraphQL query's `states: [OPEN]` clause is asserted somewhere — either via direct query-string inspection or via a fixture whose response contains a closed parent the script filters out.
- [ ] Fixture and test names accurately describe the scenarios they cover.
- [ ] No regression in the other `--list` tests' coverage (ordering, deduplication, no-slug skipping).

## Blocked by

- `github-issues-binding/15`

## Agent Brief

> *This was generated by AI during triage.*

**Summary:** The `list-archived-excluded` test's fixture contains only OPEN `formann:feature` parents, so the assertion that "two open features survive" proves only the open-only baseline — not that closed parents are filtered out. Triage decision: strengthen the script-level filter. Add a CLOSED `formann:feature`-labelled parent to the fixture, have `tracker-snapshot --list` explicitly drop closed entries from its output, and assert in `expected.json` that the closed parent is filtered out.

**Current behavior:**

- The fixture's `recorded-responses/graphql-response.json` contains only OPEN parents (`#3 alpha-feature`, `#7 beta-feature`).
- The test name claims to verify "archived (closed) parent absent — only open parents returned", but the assertion that two open features survive is just the open-only baseline.
- The promised exclusion behaviour relies entirely on the GraphQL `states: [OPEN]` clause and is not exercised by any test.

**Desired behavior:**

- The fixture's GraphQL response includes a CLOSED `formann:feature`-labelled parent alongside the two open ones.
- The `tracker-snapshot --list` script-level path drops closed entries (or otherwise asserts at the script level that closed entries never make it into output), so the test would fail if a future regression bypassed the GraphQL clause while the response still happened to contain a closed parent.
- `expected.json` asserts only the open parents are emitted; the closed parent is filtered out.

**Key interfaces:**

- The `tracker-snapshot --list` code path. Where it currently relies on the GraphQL `states: [OPEN]` clause, add (or surface) a script-level guard that excludes closed entries from output. Closed-ness is observable on the GraphQL nodes via `state == "CLOSED"`.
- The fixture at `framework/bindings/issue-tracker/github-issues/tests/fixtures/list-archived-excluded/recorded-responses/graphql-response.json` — add at least one closed `formann:feature`-labelled parent (with a slug label) in addition to the two open ones.
- The fixture's `expected.json` — assert the closed parent is absent from output (only the two open parents).

**Acceptance criteria:**

- [ ] The `list-archived-excluded` fixture includes at least one CLOSED `formann:feature`-labelled parent in the GraphQL response.
- [ ] `tracker-snapshot --list`'s code path explicitly drops closed entries — the test would fail if the script-level filter were removed, even if the GraphQL clause were also bypassed by a shim returning the closed parent.
- [ ] `expected.json` asserts the closed parent is absent from output.
- [ ] The test name accurately describes what's verified.
- [ ] No regression in the other `--list` tests' coverage (ordering, deduplication, no-slug skipping).

**Out of scope:**

- Removing the GraphQL `states: [OPEN]` clause. The clause stays as the primary filter; the script-level guard is defence-in-depth.
- Adding query-string-inspection infrastructure to the `gh` shim (that was realization (b) and was not selected).

---

## Comment — Implementation

> *This was generated by AI during implementation.*

**Evidence**

- [x] **"No test in `tracker-snapshot.bats` claims to verify archived-parent exclusion without actually exercising it…":** The `"--list: archived (closed) parent absent — only open parents returned"` test now exercises real exclusion: the fixture contains CLOSED `#5 gamma-feature` alongside the two open parents; `bats tracker-snapshot.bats` → all 16 pass.
- [x] **"The GraphQL query's `states: [OPEN]` clause is asserted somewhere…":** `grep -n 'select(.state' framework/bindings/issue-tracker/github-issues/tracker-snapshot` → `select(.state == null or .state == "OPEN")` in the `--list` jq pipeline; the fixture has a CLOSED node with `state: "CLOSED"` which the guard drops. Test fails if the guard is removed.
- [x] **"Fixture and test names accurately describe the scenarios they cover…":** Test name `"--list: archived (closed) parent absent — only open parents returned"` matches exactly what's verified. Fixture slug `list-archived-excluded` is accurate.
- [x] **"No regression in the other `--list` tests' coverage…":** `bats tracker-snapshot.bats` → 16/16 pass; ordering (`alpha-feature` first, `beta-feature` second), deduplication, no-slug-skipping all confirmed green.

**What changed:**

- `framework/bindings/issue-tracker/github-issues/tests/fixtures/list-archived-excluded/recorded-responses/graphql-response.json` — added `state: "OPEN"` to existing nodes `#3` and `#7`; added CLOSED node `#5 gamma-feature` (`state: "CLOSED"`).
- `framework/bindings/issue-tracker/github-issues/tracker-snapshot` — added `state` field to the `--list` GraphQL query; added `select(.state == null or .state == "OPEN")` as a script-level defence-in-depth filter before slug extraction.

**Decisions made:**

- Used `select(.state == null or .state == "OPEN")` rather than `select(.state == "OPEN")` to preserve compatibility with inline-fixture tests (no-slug, dupe-slug) that don't carry a `state` field; those tests test different behaviours and the null-pass-through is safe.
- `expected.json` was not changed: it already correctly asserts `["alpha-feature","beta-feature"]`.
