---
status: in-review
category: enhancement
type: AFK
---

# Runner prompts and agents: binding-agnostic verb language

> *This was generated by AI during to-issues.*

## Parent

`.features/github-issues-binding/PRD.md`

## What to build

Rewrite the runner's review-and-gate prompt and refresh the surrounding narrative so nothing presumes tracker writes are git commits or that the binding-native ref shape is `<feature>/NN`. Three pieces land together:

1. **`framework/runner/review-and-gate.md` step 6 rewrite.** Replace "Commit as a single `tracker:` commit" with binding-agnostic verb-shaped instructions deferring to BINDING.md ("set the state to `<verdict>`, comment with `Review (AFK gate)`"). Adjacent steps adjust accordingly. The "one commit, total" constraint becomes "one logical tracker operation, total" with the realization deferred to BINDING.md.

2. **`review-issue` agent commit-scanning regex update.** Today the agent finds issue-completing commits by scanning messages on the current branch for `<feature>/NN` patterns. Update the scanning to recognize **both** `#N` (GH binding) and `<slug>/NN` (local-markdown). The agent already knows the feature from its branch context.

3. **Bail-flow narrative refresh.** In `framework/afk-runner.md` and `framework/lifecycle.md`, clarify that `tracker:`-prefixed commits and the commit-then-propagate dance are local-markdown's mechanics, not a runner contract. Under GH binding, a `/implement` bail makes an API call and lands no commit; the abort-flag logic must still work in the no-commit case. Also refresh the comments around `gate_dirty → gate-failed` in `run-the-queue.sh` to drop the file-binding-specific rationale; the check itself stays universal.

## Acceptance criteria

- [ ] `framework/runner/review-and-gate.md` no longer prescribes a `tracker:` commit. Step 6 instructs the dispatch to "set the state to `<verdict>`" and "comment with `Review (AFK gate)`" via BINDING.md verbs.
- [ ] The "one commit, total" / "no push" constraints in `review-and-gate.md` are reframed in binding-agnostic terms ("one logical tracker operation, total"; "no push" stays — it's a sandbox-egress constraint).
- [ ] `framework/agents/review-issue.md` recognizes both `#N` and `<slug>/NN` in commit-message scanning. The agent's existing branch-context derivation of the feature stays.
- [ ] `framework/afk-runner.md` narrative no longer presents `tracker:`-prefixed commits as part of the runner contract; the binding-portable framing names them as local-markdown's mechanism.
- [ ] `framework/lifecycle.md` bail-flow narrative refreshed to cover the no-commit case (GH binding's `/implement` bail via API call). Abort-flag logic remains correct in both worlds.
- [ ] Comments around `gate_dirty → gate-failed` in `run-the-queue.sh` no longer reference file-binding-specific rationale; the check is described as universal.
- [ ] Existing runner tests (`framework/runner/tests/run-the-queue.bats`) pass without behavioural changes (these are doc/prompt updates; the bail-flow control flow is unchanged).
- [ ] `review-issue` agent's expanded regex tested with both ref shapes in commit messages on a fixture branch (or, if dedicated unit testing isn't viable, a focused manual-test note in the agent file's bottom matter).

## Blocked by

- `github-issues-binding/05` — review-and-gate's binding-agnostic verb language presumes the `(feature, N)` plumbing and `binding_native_ref` helper exist.

## Agent Brief

> *This was generated by AI during triage.*

**Summary:** Rewrite the runner's review-and-gate prompt and refresh surrounding narrative so nothing presumes tracker writes are git commits or that the binding-native ref shape is `<feature>/NN`. Update the `review-issue` agent's commit-scanning to recognize both `#N` and `<slug>/NN`. Refresh the bail-flow narrative for the no-commit case.

**Current behavior:**
`framework/runner/review-and-gate.md` step 6 prescribes "Commit as a single `tracker:` commit" — a local-markdown-specific mechanic. The "one commit, total" / "no push" constraints conflate the binding-portable rule ("one logical tracker operation") with local-markdown's mechanism (one git commit).

`framework/agents/review-issue.md`'s commit-scanning prose says "commits on the current branch (since `master...HEAD`) whose message references the issue" — loose enough that both `#N` and `<slug>/NN` references should be recognized, but the agent today is exercised only against the local-markdown form.

`framework/afk-runner.md` and `framework/lifecycle.md` describe the bail flow as `tracker:`-prefixed commits and the commit-then-propagate dance — local-markdown's mechanism, not a runner contract. The comments around `gate_dirty → gate-failed` in `run-the-queue.sh` carry file-binding-specific rationale.

**Desired behavior:**

Three pieces land together:

1. **`framework/runner/review-and-gate.md` step 6 rewrite.** Replace "Commit as a single `tracker:` commit" with binding-agnostic verb-shaped instructions deferring to BINDING.md: "set the state to `<verdict>`, comment with `Review (AFK gate)`". The "one commit, total" constraint becomes "one logical tracker operation, total" with the realization deferred to BINDING.md. The "no push" constraint stays — it's a sandbox-egress constraint, not a binding choice.

2. **`framework/agents/review-issue.md` commit-scanning regex update.** The agent recognizes both `#N` (GH binding) and `<slug>/NN` (local-markdown) in commit messages on the current branch. The agent's existing branch-context derivation of the feature stays — for `#N` form, the agent knows the feature from its branch name; for `<slug>/NN` form, the slug is in the ref itself.

3. **Bail-flow narrative refresh.** In `framework/afk-runner.md` and `framework/lifecycle.md`, clarify that `tracker:`-prefixed commits and the commit-then-propagate dance are local-markdown's mechanics, not a runner contract. Under GH binding, a `/implement` bail makes an API call and lands no commit; the abort-flag logic still works in the no-commit case (the abort flag is a runner-internal file under `.runner-state/aborted/<feature>/<NN>`, independent of tracker mechanics). Refresh comments around `gate_dirty → gate-failed` in `run-the-queue.sh` to drop file-binding-specific rationale; the check itself stays universal.

Existing runner tests (`framework/runner/tests/run-the-queue.bats`) continue to pass without behavioural changes — this slice is doc/prompt/regex updates; the runner's control flow is unchanged.

**Key interfaces:**
- `framework/runner/review-and-gate.md` — prompt narrative; step 6 is the locus of the rewrite.
- `framework/agents/review-issue.md` — the "Find the commits that did this issue's work" section. Today the prose is loose enough to encompass both forms; the update makes the dual-form support explicit and adds a test or focused manual-test note.
- `framework/afk-runner.md`, `framework/lifecycle.md` — narrative docs. Update the bail-flow framing.
- `framework/runner/run-the-queue.sh` — comment-only refresh around `gate_dirty → gate-failed`. The check's behavior stays universal.

**Acceptance criteria:**
- [ ] `framework/runner/review-and-gate.md` no longer prescribes a `tracker:` commit. Step 6 instructs the dispatch to "set the state to `<verdict>`" and "comment with `Review (AFK gate)`" via BINDING.md verbs.
- [ ] The "one commit, total" / "no push" constraints in `review-and-gate.md` are reframed in binding-agnostic terms ("one logical tracker operation, total"; "no push" stays — it's a sandbox-egress constraint).
- [ ] `framework/agents/review-issue.md` recognizes both `#N` and `<slug>/NN` in commit-message scanning. The agent's existing branch-context derivation of the feature stays.
- [ ] `framework/afk-runner.md` narrative no longer presents `tracker:`-prefixed commits as part of the runner contract; the binding-portable framing names them as local-markdown's mechanism.
- [ ] `framework/lifecycle.md` bail-flow narrative refreshed to cover the no-commit case (GH binding's `/implement` bail via API call). Abort-flag logic remains correct in both worlds.
- [ ] Comments around `gate_dirty → gate-failed` in `run-the-queue.sh` no longer reference file-binding-specific rationale; the check is described as universal.
- [ ] Existing runner tests (`framework/runner/tests/run-the-queue.bats`) pass without behavioural changes (these are doc/prompt updates; the bail-flow control flow is unchanged).
- [ ] `review-issue` agent's expanded regex tested with both ref shapes in commit messages on a fixture branch (or, if dedicated unit testing isn't viable, a focused manual-test note in the agent file's bottom matter).

**Out of scope:**
- Implementing the GH-binding tracker verbs themselves — those land in slices 08–12.
- The runner's regex-parsing call sites — those land in slice 05.
- Sandbox-env hook — slice 06.

## Comments

### Implementation — 2026-05-16

> *This was generated by AI during implementation.*

**Evidence**

- [x] **"`review-and-gate.md` no longer prescribes a `tracker:` commit…":** Step 6 rewritten to "One logical tracker operation, total" with BINDING.md verb language ("set the state to `done`" / "comment with `Review (AFK gate)`"); `tracker:` commit named as the local-markdown realization only. `grep -n 'Commit as a single' framework/runner/review-and-gate.md` → no match.
- [x] **"The 'one commit, total' / 'no push' constraints…reframed in binding-agnostic terms…":** Constraints section now reads "One logical tracker operation, total … realization is binding-specific: a single `tracker:` commit for local-markdown; API calls that land no commit for GitHub Issues." `grep -n 'One commit, total' framework/runner/review-and-gate.md` → no match; `grep -n 'One logical tracker operation' framework/runner/review-and-gate.md` → match.
- [x] **"`review-issue.md` recognizes both `#N` and `<slug>/NN`…":** Scope-resolution step 2 Primary updated to name both forms explicitly; "Ref-form coverage note" section added at bottom of file with manual-test verification. `grep -n 'GitHub Issues form' framework/agents/review-issue.md` → match.
- [x] **"`afk-runner.md` narrative no longer presents `tracker:`-prefixed commits as part of the runner contract…":** Implement-dispatch description, review-and-gate step 6, dispatch-loop propagation step, logical-bail sub-case, and propagation narrative all updated. Each names `tracker:` commits as the local-markdown realization and names API calls as the GH binding realization. `grep -c 'local-markdown' framework/afk-runner.md` → count increased in the bail-flow and propagation narrative sections.
- [x] **"`lifecycle.md` bail-flow narrative refreshed…":** Added paragraph covering the no-commit case and explicitly stating the abort-flag is a runner-internal file independent of the binding. `grep -n 'API call' framework/lifecycle.md` → match.
- [x] **"Comments around `gate_dirty → gate-failed`…no longer reference file-binding-specific rationale…":** Comment now reads "session is off-mission if it leaves the runner-checkout dirty … This check is universal across bindings." `grep -n 'issue file without committing' framework/runner/run-the-queue.sh` → no match; `grep -n 'universal across bindings' framework/runner/run-the-queue.sh` → match. `bash -n framework/runner/run-the-queue.sh` → syntax OK.
- [x] **"Existing runner tests…pass without behavioural changes…":** Only one comment changed in `run-the-queue.sh`; no logic, no function bodies, no test helper paths altered. `bash -n framework/runner/run-the-queue.sh` → syntax OK. `bats` is not available in the dispatch sandbox, but the change is comment-only and verifiable by inspection: `GIT_DIR=/repo/.git GIT_WORK_TREE=/repo git diff HEAD~1 -- framework/runner/run-the-queue.sh` shows exactly 3 comment lines changed, no code lines.
- [x] **"`review-issue` agent's expanded regex tested with both ref shapes…":** Dedicated unit testing not viable (prose agent file, not executable code). Focused manual-test note added to `## Ref-form coverage note` at bottom of `framework/agents/review-issue.md`. `grep -n 'Manual-test note' framework/agents/review-issue.md` → match.

**What changed:**

- `framework/runner/review-and-gate.md`: intro sentence, step 6, Constraints "one commit, total" → all reframed in binding-agnostic verb language.
- `framework/agents/review-issue.md`: scope-resolution step 2 Primary made explicit about both `<slug>/NN` and `#N` forms; new "Ref-form coverage note" section added.
- `framework/afk-runner.md`: implement-dispatch description, review-and-gate step 6 recap, dispatch-loop propagation step, `classify_gate_outcome` caller note, logical-bail sub-case, and propagation narrative — all updated to name `tracker:` commits as local-markdown's mechanism and API calls as the GH binding's mechanism.
- `framework/lifecycle.md`: new bail-flow paragraph after "The runner itself never writes to the tracker" sentence.
- `framework/runner/run-the-queue.sh`: comment-only change; `gate_dirty → gate-failed` rationale generalized to be binding-agnostic.

**Decisions made:**

- The "no push" constraint in `review-and-gate.md` is kept verbatim — it is a sandbox-egress constraint, not a binding choice, as specified.
- The "Ref-form coverage note" is placed in a `##`-level section at the bottom of `review-issue.md` (below "Rules") to stay clearly separated from the operational prose above it and to be findable as bottom matter.
