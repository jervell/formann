---
status: ready-for-agent
category: enhancement
type: AFK
---

# `tracker-snapshot` for the GitHub Issues binding

> *This was generated by AI during to-issues.*

## Parent

`.features/github-issues-binding/PRD.md`

## What to build

Ship the GH binding's `tracker-snapshot` script at `framework/bindings/issue-tracker/github-issues/tracker-snapshot`. Schema-compatible with local-markdown's JSON contract (same keys and shape; the `ref` field's value format differs — `#N` here, `<feature>/NN` for local-markdown).

Two invocation forms:

- `tracker-snapshot --list` — returns open `formann:feature`-labelled parent issues, ordered by issue number ascending (binding-specific order; documented in BINDING.md).
- `tracker-snapshot <slug>` — returns the snapshot for one feature: the parent, all its sub-issues, their bodies, and their labels. One GraphQL query per invocation.

Behaviors to implement:

- Single GraphQL query fetches the parent, sub-issues, bodies, labels in one round-trip.
- Sub-issue ordering follows the GraphQL `subIssues` connection's priority order (UI-mutable via drag-and-drop), with `#N` ascending as the tiebreaker.
- Blocker eligibility resolves from the in-memory sub-issue snapshot — no extra API calls. `#N` refs in `## Blocked by` are matched against the parent's sub-issue set; unmatched refs are conservative-false on eligibility (matching local-markdown's cross-feature behaviour).
- Archive filtering: `--list` queries `is:open label:formann:feature`, so closed (archived) parents drop out naturally.
- Slug-uniqueness cardinality cases:
  - 0 matches → emit `{"feature":"<slug>","issues":[]}`, exit 0 (mirrors local-markdown's missing-feature-dir behaviour).
  - 1 match → normal snapshot.
  - ≥2 matches → exit non-zero with stderr naming both parent issues and the recovery recipe ("remove the `formann:slug:<slug>` label from one of: #N, #M").
- Transient API failures (rate-limited, 5xx, auth-expired) surface with clear exit codes after a bounded retry. Maintainer can distinguish "binding transient failure" from "issue malformed".
- Offline-testable via a `gh` shim on `PATH` reading recorded GraphQL responses from a fixture directory.

## Acceptance criteria

- [ ] `framework/bindings/issue-tracker/github-issues/tracker-snapshot` exists and is executable; bash 3.2 compatible like local-markdown's.
- [ ] JSON output is byte-compatible with local-markdown's schema (same keys, same shape; `ref` value differs by binding).
- [ ] Snapshot's top-level `feature` field carries the slug; the `ref` field carries `#N`.
- [ ] One GraphQL query per invocation (verified by the fixture harness recording the call count).
- [ ] `--list` returns open `formann:feature` parents ordered by `#N` ascending.
- [ ] `<slug>` mode orders sub-issues by the `subIssues` connection's priority order with `#N` ascending as tiebreaker.
- [ ] Blocker eligibility resolves from the in-memory sub-issue set; unmatched `#N` refs are treated as conservative-false.
- [ ] Slug-uniqueness: 0/1/≥2 matches produce the spec'd output (empty snapshot, normal snapshot, non-zero exit with named-parent error).
- [ ] `--list` dedupes parent issues sharing a slug label and warns to stderr naming both parents.
- [ ] Transient API failures retry with backoff up to a bounded count; persistent failure exits non-zero with a distinguishable exit code.
- [ ] Bats fixtures under `framework/bindings/issue-tracker/github-issues/tests/fixtures/<scenario>/` cover: empty feature, single-issue feature, multi-issue with priority ordering + `#N` tiebreaker, archived (closed) parent excluded from `--list`, slug-collision (≥2 matches), unresolved-`#N` blocker conservative-false eligibility, transient-error retry success, transient-error retry exhaustion.
- [ ] `gh` shim on PATH (test-side) intercepts `gh api graphql` calls and serves recorded responses; the script never makes a live API call in CI.
- [ ] BINDING.md gains the snapshot contract section (schema reference, ordering rules, blocker resolution rules, slug-uniqueness contract, transient-failure handling).

## Blocked by

- `github-issues-binding/02` — binding folder must exist and the static label namespace must be defined.

## Agent Brief

> *This was generated by AI during triage.*

**Summary:** Ship the github-issues binding's `tracker-snapshot` script: JSON output byte-compatible with local-markdown's contract, one GraphQL query per invocation, sub-issue ordering by priority with `#N` tiebreaker, in-memory blocker resolution, slug-uniqueness cardinality handling, bounded transient-failure retry, and a `gh`-shim-driven fixture test suite.

**Current behavior:**
`framework/bindings/issue-tracker/local-markdown/tracker-snapshot` is the only existing implementation: bash 3.2, reads `.features/<slug>/issues/*.md` from `TRACKER_ROOT`, emits JSON with keys `feature`, `issues[]` (each with `ref`, `status`, `category`, `type`, `blocked_by`, `eligible`), supports the `--list` form that lists feature directories. The github-issues binding's `tracker-snapshot` does not exist yet. Existing test prior art lives at `framework/runner/tests/tracker-snapshot.bats` with fixtures under `framework/runner/tests/fixtures/<scenario>/`.

**Desired behavior:**
`framework/bindings/issue-tracker/github-issues/tracker-snapshot` exists, is executable, bash 3.2 compatible. JSON output is byte-compatible with local-markdown's schema — same keys, same shape. Only the `ref` field's *value format* differs by binding (`#N` here, `<feature>/NN` for local-markdown). The snapshot's top-level `feature` field carries the slug.

Two invocation forms:

- `tracker-snapshot --list` — returns open `formann:feature`-labelled parent issues, ordered by issue number ascending. Filters on `is:open label:formann:feature` so closed (archived) parents drop out. Dedupes parents sharing a slug label, writing a stderr warning naming both parents.
- `tracker-snapshot <slug>` — single GraphQL query fetches the parent, all sub-issues, their bodies, and their labels. Resolves blockers from the in-memory sub-issue set — no extra API calls. `#N` refs inside `## Blocked by` matched against the parent's sub-issue set; unmatched refs are conservative-false on eligibility (mirrors local-markdown's cross-feature-blocker behavior).

Sub-issue ordering follows the GraphQL `subIssues` connection's priority order (UI-mutable via drag-and-drop) with `#N` ascending as the tiebreaker.

Slug-uniqueness cardinality cases:

- 0 matches → emit `{"feature":"<slug>","issues":[]}`, exit 0 (mirrors local-markdown's missing-feature-dir behavior).
- 1 match → normal snapshot.
- ≥2 matches → exit non-zero. Stderr names both parent issues and the recovery recipe: "remove the `formann:slug:<slug>` label from one of: #N, #M".

Transient API failures (rate-limited, 5xx, auth-expired) retry with backoff up to a bounded count. Persistent failure exits non-zero with a distinguishable exit code so the runner's classifier can tell "binding transient failure" from "issue malformed". The script is offline-testable: a `gh` shim on PATH intercepts `gh api graphql` calls and serves recorded responses; the script never hits the live API in CI.

**Key interfaces:**
- Bash 3.2 compatibility per `framework/bindings/issue-tracker/local-markdown/tracker-snapshot`.
- Snapshot JSON shape: top-level `feature` string; `issues` array of `{ref, status, category, type, blocked_by[], eligible}`. The `ref` value is `#N` (a string including the `#` prefix). The `feature` field carries the slug, not the binding-native parent ref — every snapshot consumer already has the slug in scope.
- Test fixtures live under `framework/bindings/issue-tracker/github-issues/tests/fixtures/<scenario>/` with the same `<scenario>/{recorded-responses,expected.json}` shape that `framework/runner/tests/fixtures/*/{issues,expected.json}` uses for local-markdown.
- The `gh` shim is a test-side script placed on PATH; the production `tracker-snapshot` invokes `gh api graphql` unconditionally and the shim intercepts via PATH ordering.
- BINDING.md verb section: this slice creates "List issues in a feature" (and its `--list` variant) plus the snapshot-contract write-up. Slug-uniqueness contract documented under "Slug identity".

**Acceptance criteria:**
- [ ] `framework/bindings/issue-tracker/github-issues/tracker-snapshot` exists and is executable; bash 3.2 compatible like local-markdown's.
- [ ] JSON output is byte-compatible with local-markdown's schema (same keys, same shape; `ref` value differs by binding).
- [ ] Snapshot's top-level `feature` field carries the slug; the `ref` field carries `#N`.
- [ ] One GraphQL query per invocation (verified by the fixture harness recording the call count).
- [ ] `--list` returns open `formann:feature` parents ordered by `#N` ascending.
- [ ] `<slug>` mode orders sub-issues by the `subIssues` connection's priority order with `#N` ascending as tiebreaker.
- [ ] Blocker eligibility resolves from the in-memory sub-issue set; unmatched `#N` refs are treated as conservative-false.
- [ ] Slug-uniqueness: 0/1/≥2 matches produce the spec'd output (empty snapshot, normal snapshot, non-zero exit with named-parent error).
- [ ] `--list` dedupes parent issues sharing a slug label and warns to stderr naming both parents.
- [ ] Transient API failures retry with backoff up to a bounded count; persistent failure exits non-zero with a distinguishable exit code.
- [ ] Bats fixtures under `framework/bindings/issue-tracker/github-issues/tests/fixtures/<scenario>/` cover: empty feature, single-issue feature, multi-issue with priority ordering + `#N` tiebreaker, archived (closed) parent excluded from `--list`, slug-collision (≥2 matches), unresolved-`#N` blocker conservative-false eligibility, transient-error retry success, transient-error retry exhaustion.
- [ ] `gh` shim on PATH (test-side) intercepts `gh api graphql` calls and serves recorded responses; the script never makes a live API call in CI.
- [ ] BINDING.md gains the snapshot contract section (schema reference, ordering rules, blocker resolution rules, slug-uniqueness contract, transient-failure handling).

**Out of scope:**
- Caching snapshot output across invocations (per PRD; each call hits the API fresh).
- A second `gh`-shim flavor (HTTP mock) beyond the recorded-response fixture pattern (per PRD: YAGNI for the planned scenario coverage).
- The runner-side `take_snapshot` wrapper changes — that's slice 05.
- Other verb realizations — those land in slices 08–12.
