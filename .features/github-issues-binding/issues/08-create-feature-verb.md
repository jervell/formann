---
status: in-review
category: enhancement
type: AFK
---

# Create-a-feature verb (`/to-prd` against the GH binding)

> *This was generated by AI during to-issues.*

## Parent

`.features/github-issues-binding/PRD.md`

## What to build

Document and realize the **Create a feature** verb for the GH binding. This is the entry point for `/to-prd` and the first verb that writes to GitHub.

Verb realization (one logical operation, four steps):

1. **Pre-flight slug uniqueness check.** Run `gh issue list --label formann:slug:<slug> --state all`. If any match exists, refuse with a clear error naming the existing issue. The maintainer picks a different slug.
2. **On-demand slug-label creation.** `gh label create formann:slug:<slug> --force` — idempotent. The static label namespace is assumed pre-created via `bootstrap-labels` (see slice 02); the per-feature slug label is created on the fly.
3. **Parent issue creation.** `gh issue create --title <title> --body @prd.md --label formann:feature,formann:slug:<slug>`. Title goes in the native title field; PRD body goes in the issue body (no `# Title` duplication).
4. **Feature branch creation.** Create and switch to the feature branch (slug → branch name), per the existing local-markdown convention. This step is binding-agnostic and unchanged.

Slug-length validation: the binding's 50-char label cap leaves 37 chars for the slug (`formann:slug:` is 13 chars). `/to-prd` validates slug length at pick time and rejects early.

`/to-prd`'s SKILL.md already says "Set up the feature workspace per `docs/formann/issue-tracker/BINDING.md`" and "publish per `docs/formann/issue-tracker/BINDING.md`" — so this slice is primarily BINDING.md content. The skill stays binding-agnostic.

## Acceptance criteria

- [ ] BINDING.md's "Create a feature" verb section documents the four steps with concrete `gh` commands.
- [ ] Pre-flight uniqueness check runs against `--state all` (not just open) so re-using an archived slug is also caught.
- [ ] On slug collision, the verb refuses with a clear error naming the existing issue. No labels created, no parent issue created.
- [ ] On-demand `gh label create formann:slug:<slug> --force` runs idempotently — the verb succeeds even if the label already exists from a prior aborted attempt.
- [ ] Parent issue body is the PRD content; the title is in the native title field; no `# Title` duplication inside the body.
- [ ] Parent issue carries both `formann:feature` and `formann:slug:<slug>` labels.
- [ ] Slug-length validation: `/to-prd` rejects slugs > 37 characters at the pick step with a clear error, before any GH writes.
- [ ] BINDING.md's hard-limits table includes the 50-char label cap and names this validation as the workaround.
- [ ] End-to-end verification against a scratch repo: `/to-prd` with a fresh slug creates the parent issue, applies both labels, creates the feature branch, and switches to it. Re-running with the same slug refuses cleanly. A slug of length 38+ refuses at the pick step.

## Blocked by

- `github-issues-binding/02` — static label namespace must be bootstrapped.
- `github-issues-binding/03` — `tracker-snapshot`'s slug-uniqueness check shares the same shape as `/to-prd`'s pre-flight; landing the snapshot first ensures the contract is one consistent thing.

## Agent Brief

> *This was generated by AI during triage.*

**Summary:** Document and realize the **Create a feature** verb for the GH binding — `/to-prd`'s entry point. Pre-flight slug uniqueness check, on-demand slug-label creation, parent issue creation with PRD body and required labels, feature branch creation.

**Current behavior:**
The github-issues binding's BINDING.md (skeleton from slice 02) has empty verb sections. There is no realization of the **Create a feature** verb. `/to-prd`'s SKILL.md already says "Set up the feature workspace per `docs/formann/issue-tracker/BINDING.md`" and "publish per `docs/formann/issue-tracker/BINDING.md`" — so the skill is already binding-agnostic; what's missing is BINDING.md's GH-binding realization. GitHub labels carry no uniqueness constraint, and `gh issue create --label formann:slug:<slug>` fails with HTTP 422 if the slug label doesn't exist.

**Desired behavior:**
BINDING.md's "Create a feature" verb section documents the realization as one logical operation in four steps:

1. **Pre-flight slug uniqueness check.** Run `gh issue list --label formann:slug:<slug> --state all`. If any match exists, refuse with a clear error naming the existing issue. No labels created, no parent issue created. The maintainer picks a different slug. Running against `--state all` (not just open) catches re-use of archived slugs.
2. **On-demand slug-label creation.** `gh label create formann:slug:<slug> --force`. Idempotent — re-running succeeds if the label already exists from a prior aborted attempt. The static label namespace is assumed pre-created via `bootstrap-labels` (slice 02); the per-feature slug label is created on the fly.
3. **Parent issue creation.** `gh issue create --title <title> --body @prd.md --label formann:feature,formann:slug:<slug>`. Title goes in the native GitHub title field. PRD body goes in the parent issue body. No `# Title` duplication inside the body.
4. **Feature branch creation.** Create and switch to the feature branch (slug → branch name) per the existing local-markdown convention. Binding-agnostic and unchanged.

Slug-length validation: GitHub's 50-char label cap leaves 37 chars for the slug (`formann:slug:` is 13 chars). `/to-prd` validates slug length at pick time and rejects early with a clear error, before any GH writes.

BINDING.md's hard-limits table includes the 50-char label cap and names this validation as the workaround.

**Key interfaces:**
- BINDING.md's "Create a feature" verb section is the locus of this slice. The skill (`/to-prd`'s SKILL.md) stays binding-agnostic.
- BINDING.md's "Slug identity" section documents that the pre-flight check and `tracker-snapshot`'s slug-uniqueness check (slice 03) share the same query shape (`gh issue list --label formann:slug:<slug>` AND `formann:feature`).
- BINDING.md's hard-limits table includes the 50-char label limit and the slug-length workaround.
- The `gh` commands per step are the contract; the binding does not introduce a new wrapper script — these are direct `gh` invocations from the skill via BINDING.md's verb realization.

**Acceptance criteria:**
- [ ] BINDING.md's "Create a feature" verb section documents the four steps with concrete `gh` commands.
- [ ] Pre-flight uniqueness check runs against `--state all` (not just open) so re-using an archived slug is also caught.
- [ ] On slug collision, the verb refuses with a clear error naming the existing issue. No labels created, no parent issue created.
- [ ] On-demand `gh label create formann:slug:<slug> --force` runs idempotently — the verb succeeds even if the label already exists from a prior aborted attempt.
- [ ] Parent issue body is the PRD content; the title is in the native title field; no `# Title` duplication inside the body.
- [ ] Parent issue carries both `formann:feature` and `formann:slug:<slug>` labels.
- [ ] Slug-length validation: `/to-prd` rejects slugs > 37 characters at the pick step with a clear error, before any GH writes.
- [ ] BINDING.md's hard-limits table includes the 50-char label cap and names this validation as the workaround.
- [ ] End-to-end verification against a scratch repo: `/to-prd` with a fresh slug creates the parent issue, applies both labels, creates the feature branch, and switches to it. Re-running with the same slug refuses cleanly. A slug of length 38+ refuses at the pick step.

**Out of scope:**
- Sub-issue creation — that's slice 09's `/to-issues` realization.
- Archive — slice 12.
- Engineered enforcement of slug uniqueness (per PRD's out-of-scope) — the binding detects collisions at every point of contact but does not prevent hand-applied dupes.

## Comments

### Implementation — 2026-05-16

> *This was generated by AI during implementation.*

**Evidence**

- [x] **"BINDING.md's 'Create a feature' verb section documents…":** `grep -n '### Create a feature' framework/bindings/issue-tracker/github-issues/BINDING.md` → line 66; section covers pre-flight slug-length check, Step 1 (uniqueness), Step 2 (label create), Step 3 (issue create), Step 4 (branch creation), each with concrete `gh` commands.
- [x] **"Pre-flight uniqueness check runs against `--state all`…":** `grep -n '\-\-state all' framework/bindings/issue-tracker/github-issues/BINDING.md` → line 86: `gh issue list --label "formann:slug:<slug>" --state all --json number,title`; prose on line 89 states "`--state all` returns both open and closed issues, so re-using an archived slug is also caught."
- [x] **"On slug collision, the verb refuses with a clear error naming the existing issue…":** BINDING.md lines 91–94 show the exact error message template: `Error: slug "<slug>" is already in use by issue #<N> ("<title>"). Choose a different slug.`; line 96 states "No labels are created and no parent issue is opened."
- [x] **"On-demand `gh label create formann:slug:<slug> --force` runs idempotently…":** `grep -n '\-\-force' framework/bindings/issue-tracker/github-issues/BINDING.md` → line 101: `gh label create "formann:slug:<slug>" --force`; line 104 states "`--force` makes this idempotent: if the label already exists from a prior aborted attempt, the command succeeds without error."
- [x] **"Parent issue body is the PRD content; the title is in the native title field; no `# Title` duplication…":** BINDING.md line 115: "The title goes in GitHub's native title field. The PRD content (`@prd.md`) goes in the issue body verbatim. `# Title` is **not** duplicated inside the body."
- [x] **"Parent issue carries both `formann:feature` and `formann:slug:<slug>` labels…":** `grep -n 'formann:feature,formann:slug' framework/bindings/issue-tracker/github-issues/BINDING.md` → line 112: `--label "formann:feature,formann:slug:<slug>"`; confirmed in prose on line 115.
- [x] **"Slug-length validation: `/to-prd` rejects slugs > 37 characters at the pick step…":** BINDING.md lines 72–81 document the pre-flight slug-length validation as the first action before any API call; includes the shell snippet with `${#slug} -gt 37` check and a clear error message; line 81 states "No labels are created and no issues are opened."
- [x] **"BINDING.md's hard-limits table includes the 50-char label cap and names this validation as the workaround…":** `grep -n 'Label name length' framework/bindings/issue-tracker/github-issues/BINDING.md` → line 280: `| Label name length | 50 chars | ... | /to-prd validates slug length ≤ 37 characters at the pick step before any API call (see Create a feature); formann:slug: occupies 13 of the 50 characters |`
- [ ] **"End-to-end verification against a scratch repo…" [human]:** Run `/to-prd` against a scratch GH repo: (a) fresh slug → parent issue created with both labels, branch created and switched to; (b) same slug again → refuses with "already in use" error; (c) slug of length 38+ → refuses at the pick step with the slug-length error.

**What changed:**

- `framework/bindings/issue-tracker/github-issues/BINDING.md` — Added `### Create a feature` verb section (pre-flight slug-length check, Steps 1–4 with concrete `gh` commands); filled in the `## Hard limits` table (50-char label cap, 100-sub-issues cap, 8-level nesting cap, 65,536-char body cap).

**Decisions made:**

- Shell snippet for slug-length validation is written in bash style consistent with the binding's other scripts (`bootstrap-labels`, `tracker-snapshot`). It's illustrative, not a separate script — the verb realization is the agent's responsibility to execute.
- The "Create a feature" verb is placed after "Comment with `<kind>`" in the Tracker operations section, following the existing verb ordering and matching the PRD's verb-realizations summary.
