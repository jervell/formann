---
status: needs-triage
category: enhancement
type: AFK
---

# `sandbox-env` runner hook + GH binding's Keychain emitter

> *This was generated by AI during to-issues.*

## Parent

`.features/github-issues-binding/PRD.md`

## What to build

Two pieces ship together so the runner stays binding-agnostic while the GH binding can declare its sandbox prerequisites:

1. **Binding-agnostic runner hook.** Before `docker run`, the runner checks whether `$HOST_REPO/docs/formann/issue-tracker/sandbox-env` (a path on the role surface) is executable. If yes, invoke it and append its stdout — one `KEY=value` line per env var — to the container's `--env-file`. local-markdown ships no script; the hook is a no-op there.

2. **GH binding's `sandbox-env` script.** Ships at `framework/bindings/issue-tracker/github-issues/sandbox-env`. Retrieves the GH token from macOS Keychain (mirroring `retrieve-token.sh`'s pattern) and emits `GH_TOKEN=<token>` on stdout. Exits non-zero with a clear stderr error when the Keychain entry is absent.

This keeps `run-the-queue.sh` binding-agnostic by construction — the runner never imports `gh`, `GH_TOKEN`, or any GitHub knowledge. The binding declares its own sandbox needs.

## Acceptance criteria

- [ ] Runner-side: before `docker run`, the queue runner invokes `$HOST_REPO/docs/formann/issue-tracker/sandbox-env` if executable and appends its stdout to the container env. Local-markdown's no-script case is a no-op.
- [ ] Runner-side: malformed lines from the script (empty lines, lines without `=`, lines with shell metacharacters) are handled defensively — either skipped with a warning or rejected with a clear error. Decision documented in code comments.
- [ ] GH binding: `framework/bindings/issue-tracker/github-issues/sandbox-env` exists, is executable, and emits `GH_TOKEN=<token>` on stdout when Keychain has the entry.
- [ ] GH binding: missing-Keychain-entry case exits non-zero with a clear stderr error.
- [ ] No GH-specific knowledge in `run-the-queue.sh` (no `gh`, no `GH_TOKEN`, no Keychain code).
- [ ] Bats tests cover the GH script: Keychain-entry-present emits the expected stdout; Keychain-entry-absent surfaces the stderr error. Mirrors `retrieve-token.sh`'s existing test pattern.
- [ ] Bats tests cover the runner hook: with a fixture `sandbox-env` script on the role surface, the hook picks it up and passes env vars into the container; without one, the dispatch path is unchanged.
- [ ] BINDING.md's "Sandbox compatibility" section documents `GH_TOKEN` as the env var the binding needs and `api.github.com` as the egress requirement.

## Blocked by

- `github-issues-binding/02` — binding folder must exist for the GH-side script.
