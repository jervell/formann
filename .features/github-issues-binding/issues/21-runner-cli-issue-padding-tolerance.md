---
status: in-review
category: bug
type: AFK
---

# Make runner `--issue` lookup tolerant of `nn` zero-padding differences

> *This was generated by AI during to-issues.*

## Parent

`.features/github-issues-binding/PRD.md`

## What to build

The runner's `--issue <feature>/<NN>` argparse at `framework/runner/run-the-queue.sh:728-735` extracts the trailing integer verbatim into `$ISSUE_NN`, with no normalization. The two issue-tracker bindings emit `nn` with different padding conventions:

- **local-markdown** emits `nn` lifted verbatim from the filename regex `^([0-9]+)-...` — e.g., `"03"` for `03-foo.md`. Zero-padded.
- **github-issues** emits `nn` from the raw GraphQL `.number` field — e.g., `"3"` for issue `#3`. Unpadded.

`run_single` at `run-the-queue.sh:1709-1710` does exact-string jq compare:

```bash
entry="$(jq -c --arg n "$ISSUE_NN" \
  'first(.issues[] | select(.nn == $n)) // empty' <<<"$snap")"
```

So a maintainer who types `--issue github-issues-binding/03` against the github-issues binding (snapshot has `"3"`) — or `--issue self-install/3` against local-markdown (snapshot has `"03"`) — gets refused with `single-dispatch refused: missing — ref '…/03' is not present in feature '…'`. The diagnostic at line 1713 does not hint that the issue exists but with a different padding.

This is a UX footgun, not a correctness bug: typing the right padding works. But cross-binding muscle memory or copying a number from the GitHub UI (`#7`) and typing it against a local-markdown feature (filename `07-foo.md`, snapshot `"07"`) hits it. The new binding doubles the surface and the asymmetry makes the diagnostic more confusing.

Fix: compare numerically in the lookup so padding stops mattering. The contract (per `BINDING.md` field note for `nn`) is "the integer half of the runner's CLI ref form `<feature>/<NN>`" — always integer-valued — so numeric comparison is honest. Use `jq`'s `tonumber` on both sides of the compare.

## Acceptance criteria

- [ ] `framework/runner/run-the-queue.sh:1709-1710` (`run_single` lookup) compares `nn` numerically: `select((.nn | tonumber) == ($n | tonumber))`.
- [ ] Argparse at `run-the-queue.sh:728-735` is unchanged — the regex still accepts `^[0-9]+$` for the NN portion; no other changes needed there.
- [ ] `framework/runner/tests/run-the-queue.bats`: regression tests cover both directions — `--issue feature/03` resolves a snapshot entry with `nn:"3"`; `--issue feature/3` resolves a snapshot entry with `nn:"03"`.
- [ ] All existing `run-the-queue.bats` tests continue to pass.
- [ ] No other runner call sites need changing: `binding_native_ref` (line 269), `run_loop` (line 1808), and `feature_has_dispatchable_ref` (line 2034) all source `nn` from the snapshot itself, not from CLI input — they cannot have padding mismatches and should not be modified.

## Blocked by

None — can start immediately.

## Agent Brief

> *This was generated by AI during to-issues.*

**Summary:** One-line jq change in `run_single`'s lookup, plus two regression tests. The lookup compares `nn` as a string today; it should compare as a number.

**Current behavior:**

- The runner's `--issue <feature>/<NN>` argparse regex (`^([a-z0-9][a-z0-9-]*)/([0-9]+)$`) matches both `feature/3` and `feature/03`. The captured `${BASH_REMATCH[2]}` is the literal user input.
- local-markdown emits `nn` zero-padded (verbatim from filename); github-issues emits `nn` unpadded (raw GraphQL `.number`).
- `run_single`'s jq lookup `select(.nn == $n)` does exact-string compare, so the strings must match exactly.
- A user who types the "wrong" padding for the active binding sees the issue refused with `single-dispatch refused: missing`, even though the issue exists.
- The error message does not surface padding as a possible cause.

**Desired behavior:**

- `--issue feature/03` resolves a snapshot entry with `nn:"3"` (and vice versa). The runner stops caring about leading zeros in the CLI input.
- Behavior is identical for callers that already type the matching padding.
- The error message is unchanged — the failure mode it describes (issue genuinely missing) is still the right failure mode when no entry matches under either padding.

**Key interfaces:**

- `framework/runner/run-the-queue.sh:1709-1710` — the only call site that receives potentially-mismatched-padding `nn` from the user. The other three `.nn`-consuming sites (`binding_native_ref`, `run_loop`, `feature_has_dispatchable_ref`) source `nn` from the snapshot itself and never see user input, so they don't need changing.
- `framework/runner/tests/run-the-queue.bats` — add two regression tests, one per direction. Use the existing fixture pattern (snapshot stub + CLI invocation + assertion on exit code and trailing reason).

**Acceptance criteria:**

- [ ] `--issue feature/03` against a snapshot with `nn:"3"` resolves the entry and proceeds with dispatch (or fails at the next eligibility check, not at the lookup).
- [ ] `--issue feature/3` against a snapshot with `nn:"03"` resolves the entry and proceeds with dispatch.
- [ ] The runner does not regress on the existing happy paths (matching padding under either binding).
- [ ] All existing `run-the-queue.bats` tests pass.

**Out of scope:**

- Argparse normalization (storing a normalized form alongside the raw). Numeric compare in jq is a cleaner fix and avoids carrying two variables through the runner.
- Improving the "missing" diagnostic to hint at padding. With numeric compare, padding is no longer a failure mode worth diagnosing.
- Normalizing what the bindings emit. Each binding's `nn` value reflects its native identity (filename sequence vs. GitHub issue number); changing emit shape risks downstream surprises.

## Rework — round 2 — 2026-05-18

> *This was generated by the maintainer after the AFK gate's blocked verdict.*

The first round (commit `2411ab2`) fixed only the eligibility gate in `run_single`. The AFK gate's review (cherry-pick `59a7722`) found two correct objections, both of which now define the remaining scope:

1. **The original AC5 was wrong.** It asserted that `binding_native_ref` (line 269), `run_loop` (line 1808), and `feature_has_dispatchable_ref` (line 2034) all source `nn` from the snapshot and "cannot have padding mismatches." That is **true for `run_loop` and `feature_has_dispatchable_ref`**, but **false for `binding_native_ref` when called from the `run_single` path**: `run_single` (line 1745) passes the raw user-typed `ISSUE_NN` to `dispatch_one`, which (line 1446) passes the same value to `binding_native_ref`. The string-compare at line 269 then fails the cross-padding case, and `dispatch_one` returns 1 with `RUN_STOP_REASON="snapshot-failed-mid-dispatch:pre"`. So the original fix moves the failure site from line 1710 to line 269 with a more confusing error — the cross-padding scenario still does not work end-to-end.

2. **The regression tests did not exercise the real path.** Both tests rely on `setup_run_single_test`, which stubs `dispatch_one` (line 1126-1129). The stub never calls `binding_native_ref`, so the failing string compare never runs in the test. The tests pass against a broken end-to-end path.

### Remaining acceptance criteria (supersede original AC3 and AC5)

- [ ] **Apply the numeric-compare fix to `binding_native_ref` at `framework/runner/run-the-queue.sh:269`** as well. Same change shape: `select(.nn == $n)` → `select((.nn | tonumber) == ($n | tonumber))`. This is the second call site that receives user-typed `nn` through the `--issue` flag.
- [ ] **`run_loop` (line 1808) and `feature_has_dispatchable_ref` (line 2034) remain unchanged** — they source `nn` from the snapshot itself and never see CLI input, so they genuinely cannot have padding mismatches. The original AC5 was right about these two; only `binding_native_ref` was misclassified.
- [ ] **End-to-end regression test that exercises the real `dispatch_one` → `binding_native_ref` path** — not the stub. One of the following shapes is acceptable:
  - A new test that calls `binding_native_ref` directly with a cross-padding `nn` against a stubbed snapshot, and asserts the lookup resolves the entry.
  - A new test that drives `run_single` → real `dispatch_one` far enough to reach `binding_native_ref` (using existing test scaffolding patterns for `dispatch_one` end-to-end coverage, e.g. the pattern at `run-the-queue.bats:540+` that drives real `dispatch_one` without docker/claude).
  - The two existing tests added in round 1 (`run_single — padded CLI nn resolves unpadded snapshot entry` and the reverse) should stay — they correctly pin the eligibility-gate fix at line 1710. The new test is **in addition**, not a replacement.
- [ ] All existing `run-the-queue.bats` tests continue to pass (excluding the one pre-existing unrelated failure: `dispatch_one — pre-implement snapshot failure`).

### Why both fixes are needed

The eligibility gate at line 1710 and the dispatch-time resolver at line 269 are independent code sites that each receive `ISSUE_NN` and each do their own string-compare. Both must use numeric compare for the user-facing behavior (cross-padding `--issue` calls resolve and dispatch) to actually work. Round 1 fixed only the eligibility gate; round 2 closes the dispatch site.

### What carries forward unchanged from round 1

- The `run_single` lookup fix at line 1710 (commit `2411ab2`) is correct and stays.
- The two regression tests added in round 1 are correct and stay (they pin the gate-level fix).
- Argparse, `run_loop`, and `feature_has_dispatchable_ref` remain untouched.
- The "Out of scope" list (argparse normalization, diagnostic improvements, bindings emit-shape changes) still applies.

## Comments

### Implementation — 2026-05-18

> *This was generated by AI during implementation.*

**Evidence**

- [x] **"`run-the-queue.sh:1709-1710` (`run_single` lookup) compares `nn` numerically…":** changed `select(.nn == $n)` to `select((.nn | tonumber) == ($n | tonumber))` in `framework/runner/run-the-queue.sh`; build green (commit 2411ab2).
- [x] **"Argparse at `run-the-queue.sh:728-735` is unchanged…":** `grep -n 'BASH_REMATCH\[2\]' framework/runner/run-the-queue.sh` → line 734, unchanged; argparse regex untouched.
- [x] **"`framework/runner/tests/run-the-queue.bats`: regression tests cover both directions…":** covered by `run_single — padded CLI nn resolves unpadded snapshot entry (03 vs 3)` (ok 55) and `run_single — unpadded CLI nn resolves padded snapshot entry (3 vs 03)` (ok 56); both pass.
- [x] **"All existing `run-the-queue.bats` tests continue to pass…":** 173 of 174 tests pass; the one failure (`dispatch_one — pre-implement snapshot failure`) is pre-existing on HEAD before this change (confirmed via `git stash` / re-run).
- [x] **"No other runner call sites need changing…":** `grep -n '\.nn' framework/runner/run-the-queue.sh` → `binding_native_ref` (line 269), `run_loop` (line 1808), `feature_has_dispatchable_ref` (line 2034) all source `nn` from the snapshot directly; none modified.

**What changed:**

Single jq predicate in `run_single` (`framework/runner/run-the-queue.sh:1710`): string equality `select(.nn == $n)` → numeric equality `select((.nn | tonumber) == ($n | tonumber))`. Two regression tests added to `framework/runner/tests/run-the-queue.bats`.

### Review (AFK gate) — 2026-05-18

> *This was generated by AI during runner-side review.*

The analysis is confirmed. Line 1745 passes `$ISSUE_NN` (user-typed) to `dispatch_one`. `dispatch_one` then passes this same `$nn` to `binding_native_ref` (line 1446). `binding_native_ref` does a string comparison (`select(.nn == $n)` at line 269). With a cross-padding case, this string comparison fails, causing `dispatch_one` to return 1 with `RUN_STOP_REASON="snapshot-failed-mid-dispatch:pre"`.

The regression tests stub `dispatch_one` so they never exercise this failure path.

---

**Scope resolved:**

- Commit `2411ab2` — "runner: compare nn numerically in run_single lookup"
- Files changed: `framework/runner/run-the-queue.sh` (1 line), `framework/runner/tests/run-the-queue.bats` (38 lines)
- Commit `ac22381` — tracker state update (no code change)

---

## Findings

**Finding 1: The fix is incomplete — `binding_native_ref` still uses string comparison when called from the `run_single` path**

Location: `/repo/framework/runner/run-the-queue.sh`, lines 1445-1451 (in `dispatch_one`) and line 269 (in `binding_native_ref`)

Problem: `run_single` calls `dispatch_one "$ISSUE_FEATURE" "$ISSUE_NN"` at line 1745, passing the raw user-typed `ISSUE_NN`. Inside `dispatch_one`, line 1446 calls `binding_native_ref "$feature" "$nn" "$pre_json"` with that same user-typed value. `binding_native_ref` does `select(.nn == $n)` — string comparison — against the pre-dispatch snapshot. If the user typed `--issue github-issues-binding/03` but the snapshot has `nn:"3"`, this string comparison finds nothing, `binding_native_ref` returns 1, and `dispatch_one` sets `RUN_STOP_REASON="snapshot-failed-mid-dispatch:pre"` and returns 1. The `run_single` eligibility gate passes (the fix works there), but dispatch fails with a misleading "snapshot-failed-mid-dispatch:pre" instead of succeeding. The cross-padding scenario that the fix was intended to address still fails end-to-end at runtime, just at a different point.

Severity: 🔴 Critical

This is the core correctness failure: the stated goal of the issue (letting users type either padding and have dispatch succeed) is not achieved. The fix moves the failure site from the eligibility gate to the dispatch phase, and changes the error message, but does not make the feature work.

**Finding 2: Regression tests do not expose the remaining failure path**

Location: `/repo/framework/runner/tests/run-the-queue.bats`, lines 1226-1262 (both new tests)

Problem: Both new regression tests stub out `dispatch_one` (via `setup_run_single_test` at line 1126-1129). They verify that the eligibility gate passes (exit 0, `RUN_STOP_REASON = "single-dispatch (success)"`, `TEST_DISPATCH_CALLED` file non-empty). But the stubbed `dispatch_one` does not call `binding_native_ref`, so the failing string comparison never executes in the test. The tests pass and claim the fix works, but the real `dispatch_one` → `binding_native_ref` path is never tested. This is not a problem with the test structure per se (the surrounding tests use the same stub pattern appropriately), but it means neither test actually validates the end-to-end padded-dispatch scenario.

Severity: 🟡 Important

The tests verify the correct layer (the eligibility gate), but they create a false confidence that the issue is fully resolved when it is not.

---

## Verification summary

**Checked:**

- Commit `2411ab2` diff in full — only two files changed as claimed.
- `run-the-queue.sh` line 1710: jq predicate changed from `select(.nn == $n)` to `select((.nn | tonumber) == ($n | tonumber))` — correct, as specified.
- Argparse block lines 728-735: unchanged; `BASH_REMATCH[2]` at line 734; regex `^([0-9]+)$` for the NN portion intact — matches AC2.
- `binding_native_ref` (line 269): unchanged, still uses string comparison — matches AC5 as literally stated.
- `run_loop` (line 1808): unchanged — matches AC5.
- `feature_has_dispatchable_ref` (line 2034): unchanged — matches AC5.
- `tonumber` edge-case safety: `ISSUE_NN` is guaranteed `[0-9]+` by argparse regex; snapshot `nn` is always all-digit strings from both bindings (verified in `tracker-snapshot` source and github-issues BINDING.md). No empty-string or non-numeric input is reachable.
- Call chain traced: `run_single` → `dispatch_one` (line 1745 passes `ISSUE_NN`) → `binding_native_ref` (line 1446 passes `nn`) → string comparison at line 269. Padding mismatch still causes failure here.
- Test stubs: `setup_run_single_test` stubs `dispatch_one` unconditionally; both new tests use this setup; the real `binding_native_ref` is never exercised.
- Evidence block claims verified against code: the five checked items in the Evidence block are all accurate for what they claim, but the claim "build green" is based on tests that stub the failing path.

**Pending human verification:**

None — all acceptance criteria were of the automated/code kind with no `[human]` items.

### Implementation — round 2 — 2026-05-18

> *This was generated by AI during implementation.*

**Evidence**

- [x] **"Apply the numeric-compare fix to `binding_native_ref`…":** changed `select(.nn == $n)` to `select((.nn | tonumber) == ($n | tonumber))` at `framework/runner/run-the-queue.sh:269`; covered by `binding_native_ref — padded nn resolves unpadded snapshot entry (03 vs 3)` and `… unpadded nn resolves padded snapshot entry (3 vs 03)`; both pass.
- [x] **"`run_loop` (line 1808) and `feature_has_dispatchable_ref` (line 2034) remain unchanged…":** `git diff 107c150..HEAD -- framework/runner/run-the-queue.sh` shows only the single edit at line 269; `run_loop` and `feature_has_dispatchable_ref` untouched.
- [x] **"End-to-end regression test that exercises the real `dispatch_one` → `binding_native_ref` path…":** chose the first acceptable shape — direct unit test on `binding_native_ref` against stubbed snapshots, with no `dispatch_one` stub in the path. Red verified pre-fix (`runner: binding_native_ref: (f, 03) not found in snapshot` for both new tests); green post-fix.
- [x] **"All existing `run-the-queue.bats` tests continue to pass…":** 175 of 176 pass; the one failure (`dispatch_one — pre-implement snapshot failure sets named stop reason and records iteration`, test #48) is pre-existing — confirmed by `git stash` of working-tree changes and re-running the filter, which still fails identically.

**What changed:**

One-line jq predicate at `framework/runner/run-the-queue.sh:269`: string equality → numeric equality, matching the round-1 fix at line 1710. Two regression tests added to the existing `binding_native_ref — pure lookup` test block in `framework/runner/tests/run-the-queue.bats`, mirroring the directional shape of the round-1 `run_single` tests but bound to the real `binding_native_ref` function.

**Decisions made:**

- Test-shape choice: round-2 AC3 listed three acceptable test shapes. Picked shape 1 (direct unit test on `binding_native_ref` with cross-padding `nn`). It exercises the previously-broken code path without scaffolding overhead, and the two new tests sit cleanly next to the existing `binding_native_ref` unit tests instead of in the `run_single` stub-heavy block.
- TDD order: initial pass jumped to the fix before the tests. Reverted the fix, ran the new tests to confirm red (both failed with `not found in snapshot`), then re-applied the fix and confirmed green. The committed diff is the green-state code.