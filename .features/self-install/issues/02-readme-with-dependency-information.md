---
status: ready-for-agent
category: enhancement
type: AFK
---

# README with dependency information

> *This was generated by AI during to-issues.*

## Parent

`.features/self-install/PRD.md`

## What to build

Rewrite Formann's top-level `README.md` to serve two audiences cleanly: someone evaluating Formann as a consumer, and someone contributing to Formann itself. Cover what Formann is (one paragraph, linking to `CONTEXT.md` and `docs/adr/`), consumer adoption (one-paragraph pointer to `installer/README.md`, no duplication), contributor setup (clone, run `./installer/install.sh .` to dogfood, what the resulting checkout gives you — `/triage`, `/implement`, AFK runner from inside Formann), and a dependency section split by audience.

The file remains human-only — no embedded YAML, no frontmatter, no structured data — per the convention established at extraction time.

## Acceptance criteria

- [ ] README has sections (or clear equivalents) for: "What is Formann?", "Using Formann (consumer)", "Developing Formann (contributor)", "Dependencies"
- [ ] "What is Formann?" is one paragraph, with links to `CONTEXT.md` and `docs/adr/`
- [ ] Consumer section points readers at `installer/README.md` without duplicating its content
- [ ] Contributor section walks through: clone, `./installer/install.sh .`, what the dogfood checkout gives you (framework skills/agents discoverable, AFK runner usable)
- [ ] Dependencies section has two subsections: "To use Formann (consumer)" and "To develop Formann (contributor)"
- [ ] Consumer-host subsection lists `bash`, `git`, and optional Docker (only required for the AFK runner). Notes that the runner Dockerfile template bakes in JDK 21, Maven, Node 20, and the Claude CLI — those are container-side, not host-side, and consumers can edit the Dockerfile to suit their toolchain.
- [ ] Contributor-host subsection lists the consumer-host dependencies plus `bats` (for the installer test suite at `installer/tests/`).
- [ ] Versions are pinned to "what works today" — no speculative minimum-version exercise
- [ ] README contains no embedded YAML, frontmatter, or other structured data

## Blocked by

None — can start immediately.

## Agent Brief

> *This was generated by AI during triage.*

**Summary:** Rewrite the top-level `README.md` to serve consumer-evaluators and contributors with clear setup and dependency information, structured as a public-facing entry document.

**Current behavior:**
`README.md` is a hero image plus three lines: title, one-sentence pitch ("Methodology and tooling for running autonomous coding agents on tracked issues — keeps work moving while you're away from keyboard."), and "Early extraction from a host project. Expect churn." No setup, no dependencies, no contributor guidance, no pointer to `installer/README.md` or `CONTEXT.md`.

**Desired behavior:**
The README opens with the hero image (precedent already set), then four sections:

1. **What is Formann?** — One paragraph stating what Formann is and what problem it solves, with relative links to `CONTEXT.md` (vocabulary) and `docs/adr/` (architectural decisions). The existing "early extraction, expect churn" disclaimer may be integrated here or kept as a sibling caveat.

2. **Using Formann (consumer)** — One paragraph pointing at `installer/README.md` for setup mechanics. Do not duplicate the installer README's content (steps, env vars, examples). State the audience (someone whose project will adopt Formann) and the destination (the installer README).

3. **Developing Formann (contributor)** — Walks through: clone the repo, run `./installer/install.sh .` from the repo root, and what the resulting checkout gives the contributor — framework skills become discoverable to Claude Code via `.claude/skills/`, the AFK runner works from inside Formann, the issue tracker under `.features/` is the canonical place for ongoing Formann work. Mention that the installer's products are gitignored in self-install mode (handled automatically by the installer).

4. **Dependencies** — Two subsections distinguishing host requirements from container-bundled tools:

   - **To use Formann (consumer-host):** `bash`, `git`. Docker is optional and only required for the AFK runner. Note explicitly that the runner Dockerfile template bakes in JDK 21, Maven, Node 20, and the Claude CLI — these are container-side, not host-side, and a consumer can edit `runner/Dockerfile` to swap toolchains. The consumer's own project toolchain (e.g. Maven for a Java project) is the consumer's concern, not Formann's.
   - **To develop Formann (contributor-host):** everything above, plus `bats` (the installer test suite at `installer/tests/install.bats`).

Version pinning: list what's known to work today (e.g. "bats 1.x", "bash 4+ for arrays and parameter expansion"). Don't construct speculative minimum-version matrices.

**Key interfaces:**
- README is the public landing surface — its job is to orient, not to be the canonical reference. Push detail down to `installer/README.md`, `CONTEXT.md`, `docs/adr/`.
- Cross-references: relative links to `CONTEXT.md`, `docs/adr/`, `installer/README.md`. Don't link to specific ADR filenames or numbers — those get reordered. Link the directory.
- Hero image at `docs/images/hero.png` stays at the top — visual landing, precedent set.
- The file is human-only — no embedded YAML, no frontmatter, no structured data. (CLAUDE.md owns the "README is for humans" convention; this issue honors it.)

**Acceptance criteria:**
- [ ] README has sections (or clear heading equivalents) for: "What is Formann?", "Using Formann (consumer)", "Developing Formann (contributor)", "Dependencies"
- [ ] "What is Formann?" is one paragraph, with relative links to `CONTEXT.md` and `docs/adr/`
- [ ] Consumer section points readers at `installer/README.md` without duplicating its content
- [ ] Contributor section walks through: clone → `./installer/install.sh .` → what the dogfood checkout gives you (skill discovery, AFK runner, `.features/` tracker)
- [ ] Dependencies section splits "consumer-host" from "contributor-host" subsections
- [ ] Consumer-host subsection lists `bash`, `git`, optional Docker (AFK-runner-only). Explicitly distinguishes container-bundled tools (JDK 21, Maven, Node 20, Claude CLI in the Dockerfile template) from host-side requirements.
- [ ] Contributor-host subsection adds `bats` (installer test suite)
- [ ] No speculative minimum-version exercise — only "what works today" pinning
- [ ] Hero image (`docs/images/hero.png`) stays at the top of the file
- [ ] No embedded YAML, frontmatter, or other structured data anywhere in the file
- [ ] Existing "Early extraction... Expect churn." disclaimer is preserved (in the new structure) or integrated into the new "What is Formann?" paragraph

**Out of scope:**
- Migration guide for consumers (no prior version to migrate from)
- Sister-company onboarding polish (FAQs, screencasts, organization-fit guidance — out of scope per the formann-extraction PRD)
- LICENSE selection (deferred per the maintainer's note in the extraction PRD)
- Marketing copy beyond the existing pitch
- Documenting the symlink graph in detail (lives in `installer/README.md` and ADRs)
- Adding badges, build status, or other metadata widgets
