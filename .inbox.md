# Inbox

- 2026-05-18 GitHub issue-tracker binding: let the agent and AFK runner post under a separate identity, so maintainer vs agent activity is visually distinguishable on GitHub
- 2026-05-26 Body-template rules (Gist sentence count, no code spans, prose-only) live in `framework/skills/to-issues/SKILL.md`, but the Create-a-standalone-issue verb in `BINDING.md` and the project section of `CLAUDE.md` don't link to them. An agent acting on a natural-language "create a standalone issue" request has no breadcrumb to the rules and ships off-spec Gists (see #40's and #42's original Gists). Decide: pointer-add at both citation sites, or extract the body-template rules to a binding-agnostic location both verbs cite.
- 2026-05-26 Is is possible to write a deterministic script that determines dependencies between CLAUDE.md, skills and agents? Including skill-skill dependencies, skill-CLAUDE.md dependencies, skill-agent dependencies, agent-agent dependencies, agent-CLAUD.md dependencies etc? This would be useful for discovering unwanted dependencies, loops, understanding the impact of changes and many other things.
- 2026-06-09 The smoke runbook says `RUNNER_TRANSPORT_RETRY_BACKOFFS` / `RUNNER_TRANSPORT_RETRY_MAX_ATTEMPTS` can be shrunk via env so walks skip the full wait, but they're plain assignments at `run-the-queue.sh:76-77` (not `: "${VAR:=…}"`), so env overrides are ignored — the #71 scenario-3 walk ran the full 30/90/240s schedule (~18 min). Decide: make them env-overridable (like `RUNNER_DISABLE_TRANSPORT_RETRY` does) so walks can shrink the wait, and/or correct the runbook's claim.