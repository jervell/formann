---
status: ready-for-agent
category: enhancement
type: AFK
---

# Stamp smoke-iterate marker

## Parent

`.features/smoke-iterate/PRD.md`

## What to build

Create a one-line marker file at
`.features/smoke-iterate/markers/MARKER-01.txt` with the exact content:

```
smoke-iterate/01 was here
```

(no trailing blank line). Commit the new file together with the
`tracker:` status move under a single commit message
`smoke-iterate/01: stamp smoke marker`. No tests, no other changes.

## Acceptance criteria

- [ ] `.features/smoke-iterate/markers/MARKER-01.txt` exists with the expected
      single line of content
- [ ] The commit message is `smoke-iterate/01: stamp smoke marker`
- [ ] No files outside `.features/smoke-iterate/markers/` and the tracker move
      are touched

## Blocked by

(none)

## Agent Brief

> *This was generated by AI during triage.*

**Category:** enhancement

**Summary:** Trivial fixture issue — write a one-line marker file and
commit. Exists to exercise the unrolled iterate manifest composition
during the smoke-iterate smoke walk, not to do useful work.

**Resolved during triage:**

- The marker file's directory may not exist yet. `mkdir -p
  .features/smoke-iterate/markers/` before writing.
- The commit must include both the marker file and the tracker move
  in a single commit (matches the project's `/implement` convention).

**Out of scope:**

- Anything beyond writing the marker and moving the tracker.
